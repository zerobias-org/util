/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { Schedule } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace CronApi {
}

/**
 * CronApi - interface
 * @export
 * @interface CronApi
 */
export interface CronApi {
  /**
   * 
   * @summary Delete a Schedule by its id
   * @param {string} id 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.deleteSchedule('id');
   * ```
   * @openapi operation=deleteSchedule path=/crons/{id} method=DELETE
   * @memberof CronApi
   */
  deleteSchedule(id: string): Promise<void>;

  /**
   * 
   * @summary Gets a Schedule by its id
   * @param {string} id 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.getSchedule('id');
   * ```
   * @openapi operation=getSchedule path=/crons/{id} method=GET
   * @memberof CronApi
   */
  getSchedule(id: string): Promise<Schedule>;

  /**
   * 
   * @summary Schedules a Cron Job for execution
   * @param {Schedule} schedule The cron body
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.schedule(null);
   * ```
   * @openapi operation=schedule path=/crons method=PUT
   * @memberof CronApi
   */
  schedule(schedule: Schedule): Promise<Schedule>;

}

/**
 * CronProducerApi - interface
 * Producer interface for Cron
 * @export
 * @interface CronProducerApi
 */
export interface CronProducerApi {
  /**
  * 
  * @summary Delete a Schedule by its id
  * @param {string} id 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=deleteSchedule path=/crons/{id} method=DELETE
  * @memberof Cron
  */
  deleteSchedule(id: string): Promise<void>;
  /**
  * 
  * @summary Gets a Schedule by its id
  * @param {string} id 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getSchedule path=/crons/{id} method=GET
  * @memberof Cron
  */
  getSchedule(id: string): Promise<Schedule>;
  /**
  * 
  * @summary Schedules a Cron Job for execution
  * @param {Schedule} schedule The cron body
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=schedule path=/crons method=PUT
  * @memberof Cron
  */
  schedule(schedule: Schedule): Promise<Schedule>;
}

export class CronApiMappingImpl implements CronApi {
  private producer: CronProducerApi;
  constructor(producer: CronProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Delete a Schedule by its id
   * @param id 
   * @openapi operation=deleteSchedule path=/crons/{id} method=DELETE
   */
  public async deleteSchedule(id: string): Promise<void> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined || id === '') {
      throw new ParameterRequiredError('id');
    }
    return this.producer.deleteSchedule(id);
  }
  /**
   * 
   * @summary Gets a Schedule by its id
   * @param id 
   * @openapi operation=getSchedule path=/crons/{id} method=GET
   */
  public async getSchedule(id: string): Promise<Schedule> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined || id === '') {
      throw new ParameterRequiredError('id');
    }
    return this.producer.getSchedule(id);
  }
  /**
   * 
   * @summary Schedules a Cron Job for execution
   * @param schedule The cron body
   * @openapi operation=schedule path=/crons method=PUT
   */
  public async schedule(schedule: Schedule): Promise<Schedule> {
    // verify required parameter 'schedule' is not null or undefined
    if (schedule === null || schedule === undefined) {
      throw new ParameterRequiredError('schedule');
    }
    return this.producer.schedule(schedule);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapCronProducer(producer: CronProducerApi): CronApi {
  return new CronApiMappingImpl(producer);
}

/**
 * CronApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class CronApiHttpImpl implements CronProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async deleteSchedule(id: string): Promise<void> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/crons/{id}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.delete(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, '')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async getSchedule(id: string): Promise<Schedule> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/crons/{id}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Schedule')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async schedule(schedule: Schedule): Promise<Schedule> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      ...schedule,
  };
  let _path = '/crons';
  return this.client.put(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Schedule')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { EnsureRequestPrincipal, ServiceAccount, User } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace PrincipalApi {
}

/**
 * PrincipalApi - interface
 * @export
 * @interface PrincipalApi
 */
export interface PrincipalApi {
  /**
   * 
   * @summary Ensures request user or service account principal on apps pointed database
   * @param {EnsureRequestPrincipal} ensureRequestPrincipal Principal information to ensure
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.ensureRequestPrincipal(null);
   * ```
   * @openapi operation=ensureRequestPrincipal path=/requestPrincipals method=POST
   * @memberof PrincipalApi
   */
  ensureRequestPrincipal(ensureRequestPrincipal: EnsureRequestPrincipal): Promise<User | ServiceAccount>;

}

/**
 * PrincipalProducerApi - interface
 * Producer interface for Principal
 * @export
 * @interface PrincipalProducerApi
 */
export interface PrincipalProducerApi {
  /**
  * 
  * @summary Ensures request user or service account principal on apps pointed database
  * @param {EnsureRequestPrincipal} ensureRequestPrincipal Principal information to ensure
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=ensureRequestPrincipal path=/requestPrincipals method=POST
  * @memberof Principal
  */
  ensureRequestPrincipal(ensureRequestPrincipal: EnsureRequestPrincipal): Promise<User | ServiceAccount>;
}

export class PrincipalApiMappingImpl implements PrincipalApi {
  private producer: PrincipalProducerApi;
  constructor(producer: PrincipalProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Ensures request user or service account principal on apps pointed database
   * @param ensureRequestPrincipal Principal information to ensure
   * @openapi operation=ensureRequestPrincipal path=/requestPrincipals method=POST
   */
  public async ensureRequestPrincipal(ensureRequestPrincipal: EnsureRequestPrincipal): Promise<User | ServiceAccount> {
    // verify required parameter 'ensureRequestPrincipal' is not null or undefined
    if (ensureRequestPrincipal === null || ensureRequestPrincipal === undefined) {
      throw new ParameterRequiredError('ensureRequestPrincipal');
    }
    return this.producer.ensureRequestPrincipal(ensureRequestPrincipal);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapPrincipalProducer(producer: PrincipalProducerApi): PrincipalApi {
  return new PrincipalApiMappingImpl(producer);
}

/**
 * PrincipalApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class PrincipalApiHttpImpl implements PrincipalProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async ensureRequestPrincipal(ensureRequestPrincipal: EnsureRequestPrincipal): Promise<User | ServiceAccount> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      ...ensureRequestPrincipal,
  };
  let _path = '/requestPrincipals';
  return this.client.post(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'User | ServiceAccount')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { JobCompletionDetails, JobError } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace JobApi {
}

/**
 * JobApi - interface
 * @export
 * @interface JobApi
 */
export interface JobApi {
  /**
   * Callback to report cron job errors
   * @summary Callback to report cron job errors
   * @param {UUID} id 
   * @param {JobError} [jobError] 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.handleError('00000000-0000-0000-0000-000000000000', null);
   * ```
   * @openapi operation=handleError path=/job/{id}/error method=POST
   * @memberof JobApi
   */
  handleError(id: UUID, jobError?: JobError): Promise<void>;

  /**
   * Trigger job to run linker on all batches
   * @summary Trigger job to run linker on all batches
   * @param {UUID} jobId 
   * @param {UUID} ownerId 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.linkJobBatches('00000000-0000-0000-0000-000000000000', '00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=linkJobBatches path=/job/{jobId}/link method=POST
   * @memberof JobApi
   */
  linkJobBatches(jobId: UUID, ownerId: UUID): Promise<void>;

  /**
   * Callback to report cron job complete
   * @summary Callback to report cron job complete
   * @param {UUID} id 
   * @param {JobCompletionDetails} jobCompletionDetails 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.onComplete('00000000-0000-0000-0000-000000000000', null);
   * ```
   * @openapi operation=onComplete path=/job/{id}/complete method=PUT
   * @memberof JobApi
   */
  onComplete(id: UUID, jobCompletionDetails: JobCompletionDetails): Promise<void>;

}

/**
 * JobProducerApi - interface
 * Producer interface for Job
 * @export
 * @interface JobProducerApi
 */
export interface JobProducerApi {
  /**
  * Callback to report cron job errors
  * @summary Callback to report cron job errors
  * @param {UUID} id 
  * @param {JobError} [jobError] 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=handleError path=/job/{id}/error method=POST
  * @memberof Job
  */
  handleError(id: UUID, jobError?: JobError): Promise<void>;
  /**
  * Trigger job to run linker on all batches
  * @summary Trigger job to run linker on all batches
  * @param {UUID} jobId 
  * @param {UUID} ownerId 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=linkJobBatches path=/job/{jobId}/link method=POST
  * @memberof Job
  */
  linkJobBatches(jobId: UUID, ownerId: UUID): Promise<void>;
  /**
  * Callback to report cron job complete
  * @summary Callback to report cron job complete
  * @param {UUID} id 
  * @param {JobCompletionDetails} jobCompletionDetails 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=onComplete path=/job/{id}/complete method=PUT
  * @memberof Job
  */
  onComplete(id: UUID, jobCompletionDetails: JobCompletionDetails): Promise<void>;
}

export class JobApiMappingImpl implements JobApi {
  private producer: JobProducerApi;
  constructor(producer: JobProducerApi) {
    this.producer = producer;
  }

  /**
   * Callback to report cron job errors
   * @summary Callback to report cron job errors
   * @param id 
   * @param jobError 
   * @openapi operation=handleError path=/job/{id}/error method=POST
   */
  public async handleError(id: UUID, jobError?: JobError): Promise<void> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.handleError(id, jobError);
  }
  /**
   * Trigger job to run linker on all batches
   * @summary Trigger job to run linker on all batches
   * @param jobId 
   * @param ownerId 
   * @openapi operation=linkJobBatches path=/job/{jobId}/link method=POST
   */
  public async linkJobBatches(jobId: UUID, ownerId: UUID): Promise<void> {
    // verify required parameter 'jobId' is not null or undefined
    if (jobId === null || jobId === undefined) {
      throw new ParameterRequiredError('jobId');
    }
    // verify required parameter 'ownerId' is not null or undefined
    if (ownerId === null || ownerId === undefined) {
      throw new ParameterRequiredError('ownerId');
    }
    return this.producer.linkJobBatches(jobId, ownerId);
  }
  /**
   * Callback to report cron job complete
   * @summary Callback to report cron job complete
   * @param id 
   * @param jobCompletionDetails 
   * @openapi operation=onComplete path=/job/{id}/complete method=PUT
   */
  public async onComplete(id: UUID, jobCompletionDetails: JobCompletionDetails): Promise<void> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    // verify required parameter 'jobCompletionDetails' is not null or undefined
    if (jobCompletionDetails === null || jobCompletionDetails === undefined) {
      throw new ParameterRequiredError('jobCompletionDetails');
    }
    return this.producer.onComplete(id, jobCompletionDetails);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapJobProducer(producer: JobProducerApi): JobApi {
  return new JobApiMappingImpl(producer);
}

/**
 * JobApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class JobApiHttpImpl implements JobProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async handleError(id: UUID, jobError?: JobError): Promise<void> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      jobError,
  };
  let _path = '/job/{id}/error';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.post(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, '')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async linkJobBatches(jobId: UUID, ownerId: UUID): Promise<void> {
  const _queryParams = {
      ownerId,
  };
  const _bodyParams = {
      
  };
  let _path = '/job/{jobId}/link';
  _path = _path.replace('{jobId}', encodeURIComponent(String(jobId)));
  return this.client.post(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, '')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async onComplete(id: UUID, jobCompletionDetails: JobCompletionDetails): Promise<void> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      jobCompletionDetails,
  };
  let _path = '/job/{id}/complete';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.put(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, '')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

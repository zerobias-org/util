/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { EventUnion } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace EventApi {
}

/**
 * EventApi - interface
 * @export
 * @interface EventApi
 */
export interface EventApi {
  /**
   * 
   * @summary Handles events
   * @param {EventUnion} eventUnion The event body
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.handleEvent(null);
   * ```
   * @openapi operation=handleEvent path=/events method=POST
   * @memberof EventApi
   */
  handleEvent(eventUnion: EventUnion): Promise<void>;

}

/**
 * EventProducerApi - interface
 * Producer interface for Event
 * @export
 * @interface EventProducerApi
 */
export interface EventProducerApi {
  /**
  * 
  * @summary Handles events
  * @param {EventUnion} eventUnion The event body
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=handleEvent path=/events method=POST
  * @memberof Event
  */
  handleEvent(eventUnion: EventUnion): Promise<void>;
}

export class EventApiMappingImpl implements EventApi {
  private producer: EventProducerApi;
  constructor(producer: EventProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Handles events
   * @param eventUnion The event body
   * @openapi operation=handleEvent path=/events method=POST
   */
  public async handleEvent(eventUnion: EventUnion): Promise<void> {
    // verify required parameter 'eventUnion' is not null or undefined
    if (eventUnion === null || eventUnion === undefined) {
      throw new ParameterRequiredError('eventUnion');
    }
    return this.producer.handleEvent(eventUnion);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapEventProducer(producer: EventProducerApi): EventApi {
  return new EventApiMappingImpl(producer);
}

/**
 * EventApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class EventApiHttpImpl implements EventProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async handleEvent(eventUnion: EventUnion): Promise<void> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      ...eventUnion,
  };
  let _path = '/events';
  return this.client.post(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, '')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { PreviewModeBody } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace PipelineApi {
}

/**
 * PipelineApi - interface
 * @export
 * @interface PipelineApi
 */
export interface PipelineApi {
  /**
   * Starts a preview mode run on a given pipeline
   * @summary Starts a preview mode run on a given pipeline
   * @param {UUID} pipelineId 
   * @param {PreviewModeBody} previewModeBody 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.startPreviewRun('00000000-0000-0000-0000-000000000000', null);
   * ```
   * @openapi operation=startPreviewRun path=/pipelines/{pipelineId}/previews method=POST
   * @memberof PipelineApi
   */
  startPreviewRun(pipelineId: UUID, previewModeBody: PreviewModeBody): Promise<void>;

}

/**
 * PipelineProducerApi - interface
 * Producer interface for Pipeline
 * @export
 * @interface PipelineProducerApi
 */
export interface PipelineProducerApi {
  /**
  * Starts a preview mode run on a given pipeline
  * @summary Starts a preview mode run on a given pipeline
  * @param {UUID} pipelineId 
  * @param {PreviewModeBody} previewModeBody 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=startPreviewRun path=/pipelines/{pipelineId}/previews method=POST
  * @memberof Pipeline
  */
  startPreviewRun(pipelineId: UUID, previewModeBody: PreviewModeBody): Promise<void>;
}

export class PipelineApiMappingImpl implements PipelineApi {
  private producer: PipelineProducerApi;
  constructor(producer: PipelineProducerApi) {
    this.producer = producer;
  }

  /**
   * Starts a preview mode run on a given pipeline
   * @summary Starts a preview mode run on a given pipeline
   * @param pipelineId 
   * @param previewModeBody 
   * @openapi operation=startPreviewRun path=/pipelines/{pipelineId}/previews method=POST
   */
  public async startPreviewRun(pipelineId: UUID, previewModeBody: PreviewModeBody): Promise<void> {
    // verify required parameter 'pipelineId' is not null or undefined
    if (pipelineId === null || pipelineId === undefined) {
      throw new ParameterRequiredError('pipelineId');
    }
    // verify required parameter 'previewModeBody' is not null or undefined
    if (previewModeBody === null || previewModeBody === undefined) {
      throw new ParameterRequiredError('previewModeBody');
    }
    return this.producer.startPreviewRun(pipelineId, previewModeBody);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapPipelineProducer(producer: PipelineProducerApi): PipelineApi {
  return new PipelineApiMappingImpl(producer);
}

/**
 * PipelineApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class PipelineApiHttpImpl implements PipelineProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async startPreviewRun(pipelineId: UUID, previewModeBody: PreviewModeBody): Promise<void> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      previewModeBody,
  };
  let _path = '/pipelines/{pipelineId}/previews';
  _path = _path.replace('{pipelineId}', encodeURIComponent(String(pipelineId)));
  return this.client.post(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, '')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

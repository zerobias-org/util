/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { EnumValue, IllegalArgumentError } from '@zerobias-org/types-core-js';
import { ObjectSerializer, RequestFile } from './index.js';

/**
* Applies only when rebuilding all links (by owner/global). Not taken into account for classId/objectId rebuild. * `soft` : All opened links will be closed as of linker start time minus 5 minutes. (this to ensure the linker does not step on its own two feet) * `hard` : All links will be dropped from the table. use with care, this is not reversible * `nodelete`: is there as an option but most likely won't be used outside of some testing.
*/
export const LinkDeleteMode = {
  /**  */
  Nodelete: EnumValue.instance('LinkDeleteMode', 'Nodelete', 'nodelete'),
  /**  */
  Soft: EnumValue.instance('LinkDeleteMode', 'Soft', 'soft'),
  /**  */
  Hard: EnumValue.instance('LinkDeleteMode', 'Hard', 'hard'),

  from(val: string | number): EnumValue {
    if (LinkDeleteMode[val]) {
      return LinkDeleteMode[val];
    }
    const byValue = LinkDeleteMode.values.find((v) => v.value === val);
    if (byValue) {
      return byValue;
    }
    throw new IllegalArgumentError(`${val} is not a valid LinkDeleteMode`);
  },

  get values(): EnumValue[] {
    return Object
      .keys(LinkDeleteMode)
      .filter((k) => k !== 'values' && k !== 'from')
      .map((t) => LinkDeleteMode[t]);
  }
} as const;

export type LinkDeleteModeDef = typeof LinkDeleteMode[keyof typeof LinkDeleteMode];

/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { EnumValue, IllegalArgumentError } from '@zerobias-org/types-core-js';
import { ObjectSerializer, RequestFile } from './index.js';

/**
* Status of Audit
*/
export const AuditMessageLevel = {
  /**  */
  Audit: EnumValue.instance('AuditMessageLevel', 'Audit', 'audit'),
  /**  */
  EvidenceAssessment: EnumValue.instance('AuditMessageLevel', 'EvidenceAssessment', 'evidence_assessment'),

  from(val: string | number): EnumValue {
    if (AuditMessageLevel[val]) {
      return AuditMessageLevel[val];
    }
    const byValue = AuditMessageLevel.values.find((v) => v.value === val);
    if (byValue) {
      return byValue;
    }
    throw new IllegalArgumentError(`${val} is not a valid AuditMessageLevel`);
  },

  get values(): EnumValue[] {
    return Object
      .keys(AuditMessageLevel)
      .filter((k) => k !== 'values' && k !== 'from')
      .map((t) => AuditMessageLevel[t]);
  }
} as const;

export type AuditMessageLevelDef = typeof AuditMessageLevel[keyof typeof AuditMessageLevel];

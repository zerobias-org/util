/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { EnumValue, IllegalArgumentError } from '@zerobias-org/types-core-js';
import { ObjectSerializer, RequestFile } from './index.js';

/**
* Enum for a Qualification Type
*/
export const QualificationType = {
  /**  */
  Knowledge: EnumValue.instance('QualificationType', 'Knowledge', 'knowledge'),
  /**  */
  Skill: EnumValue.instance('QualificationType', 'Skill', 'skill'),

  from(val: string | number): EnumValue {
    if (QualificationType[val]) {
      return QualificationType[val];
    }
    const byValue = QualificationType.values.find((v) => v.value === val);
    if (byValue) {
      return byValue;
    }
    throw new IllegalArgumentError(`${val} is not a valid QualificationType`);
  },

  get values(): EnumValue[] {
    return Object
      .keys(QualificationType)
      .filter((k) => k !== 'values' && k !== 'from')
      .map((t) => QualificationType[t]);
  }
} as const;

export type QualificationTypeDef = typeof QualificationType[keyof typeof QualificationType];

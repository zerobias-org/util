/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import qs from 'qs';
import { EvidenceDefinitionExtended, EvidenceDefinitionFrameworkElementLink, EvidenceDefinitionFrameworkVersion, EvidenceDefinitionScfLinkRequiredDef, EvidenceDefinitionScfLinkScopeDef, EvidenceDefinitionWithScfLinksAndTags, NewEvidenceDefinition, PublishEvidenceStatusEnumDef, ResultsColumnOptionsWrapper, SearchEvidenceDefinitionBody, SortObject, UpdateEvidenceDefinition } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

// Helper to serialize core types (UUID, Nmtoken, URL, etc.) and EnumValues for query params
// All @zerobias-org/types-core-js types implement toJSON() for proper serialization
function serializeQueryParams(obj: any): any {
  if (obj === null || obj === undefined) return obj;
  if (typeof obj !== 'object') return obj;
  if (Array.isArray(obj)) return obj.map(serializeQueryParams);
  // Core types and EnumValues all have toJSON() - use it for serialization
  if (typeof obj.toJSON === 'function') {
    return obj.toJSON();
  }
  // Recursively process plain object properties
  const result: any = {};
  for (const key of Object.keys(obj)) {
    result[key] = serializeQueryParams(obj[key]);
  }
  return result;
}

export namespace EvidenceDefinitionApi {
}

/**
 * EvidenceDefinitionApi - interface
 * @export
 * @interface EvidenceDefinitionApi
 */
export interface EvidenceDefinitionApi {
  /**
   * Creates a new evidence definition
   * @summary Creates a new evidence definition
   * @param {NewEvidenceDefinition} newEvidenceDefinition 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.createEvidenceDefinition(null);
   * ```
   * @openapi operation=createEvidenceDefinition path=/catalog/evidenceDefinitions method=POST
   * @memberof EvidenceDefinitionApi
   */
  createEvidenceDefinition(newEvidenceDefinition: NewEvidenceDefinition): Promise<EvidenceDefinitionWithScfLinksAndTags>;

  /**
   * Delete evidence definition
   * @summary Delete evidence definition
   * @param {UUID} id 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.deleteEvidenceDefinition('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=deleteEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=DELETE
   * @memberof EvidenceDefinitionApi
   */
  deleteEvidenceDefinition(id: UUID): Promise<void>;

  /**
   * Get an evidence definition
   * @summary Get an evidence definition
   * @param {UUID} id 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.getEvidenceDefinition('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=getEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=GET
   * @memberof EvidenceDefinitionApi
   */
  getEvidenceDefinition(id: UUID): Promise<EvidenceDefinitionWithScfLinksAndTags>;

  /**
   * List framework elements that have links to evidence definition
   * @summary List framework elements that have links to evidence definition
   * @param {UUID} id 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {UUID} [frameworkVersionId] Filter by framework version
   * @param {EvidenceDefinitionScfLinkScope} [scope] Filter by scope
   * @param {EvidenceDefinitionScfLinkRequired} [required] Filter by required
   * @param {SortObject} [sort] Sort object
   * @param {string} [pageToken] Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listEvidenceDefinitionFrameworkElements('00000000-0000-0000-0000-000000000000', 1, 1, '00000000-0000-0000-0000-000000000000', null, null, null, 'pageToken');
   * ```
   * @openapi operation=listEvidenceDefinitionFrameworkElements path=/catalog/evidenceDefinitions/{id}/frameworkElements method=GET
   * @memberof EvidenceDefinitionApi
   */
  listEvidenceDefinitionFrameworkElements(id: UUID, pageNumber?: number, pageSize?: number, frameworkVersionId?: UUID, scope?: EvidenceDefinitionScfLinkScopeDef, required?: EvidenceDefinitionScfLinkRequiredDef, sort?: SortObject, pageToken?: string): Promise<PagedResults<EvidenceDefinitionFrameworkElementLink>>;

  /**
   * List framework version that have links to evidence definition
   * @summary List framework version that have links to evidence definition
   * @param {UUID} id 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {string} [pageToken] Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listEvidenceDefinitionFrameworkVersions('00000000-0000-0000-0000-000000000000', 1, 1, 'pageToken');
   * ```
   * @openapi operation=listEvidenceDefinitionFrameworkVersions path=/catalog/evidenceDefinitions/{id}/frameworkVersions method=GET
   * @memberof EvidenceDefinitionApi
   */
  listEvidenceDefinitionFrameworkVersions(id: UUID, pageNumber?: number, pageSize?: number, pageToken?: string): Promise<PagedResults<EvidenceDefinitionFrameworkVersion>>;

  /**
   * List evidence definitions
   * @summary List evidence definitions
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {string} [name] Filter by name
   * @param {PublishEvidenceStatusEnum} [status] Filter by status
   * @param {string} [sort] 
   * @param {UUID} [internalControlId] 
   * @param {string} [pageToken] Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listEvidenceDefinitions(1, 1, 'name', null, 'sort', '00000000-0000-0000-0000-000000000000', 'pageToken');
   * ```
   * @openapi operation=listEvidenceDefinitions path=/catalog/evidenceDefinitions method=GET
   * @memberof EvidenceDefinitionApi
   */
  listEvidenceDefinitions(pageNumber?: number, pageSize?: number, name?: string, status?: PublishEvidenceStatusEnumDef, sort?: string, internalControlId?: UUID, pageToken?: string): Promise<PagedResults<EvidenceDefinitionWithScfLinksAndTags>>;

  /**
   * Returns options/filters for evidence definitions
   * @summary Returns options/filters for evidence definitions
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @openapi operation=searchEvidenceDefinitionOptions path=/evidenceDefinitionSearch/options method=GET
   * @memberof EvidenceDefinitionApi
   */
  searchOptions(): Promise<ResultsColumnOptionsWrapper>;

  /**
   * Search all evidence definitions
   * @summary Search all evidence definitions
   * @param {SearchEvidenceDefinitionBody} searchEvidenceDefinitionBody 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {SortObject} [sort] 
   * @param {string} [pageToken] Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.search(null, 1, 1, null, 'pageToken');
   * ```
   * @openapi operation=searchEvidenceDefinitions path=/evidenceDefinitionSearch method=POST
   * @memberof EvidenceDefinitionApi
   */
  search(searchEvidenceDefinitionBody: SearchEvidenceDefinitionBody, pageNumber?: number, pageSize?: number, sort?: SortObject, pageToken?: string): Promise<PagedResults<EvidenceDefinitionExtended>>;

  /**
   * Update evidence definition
   * @summary Update evidence definition
   * @param {UUID} id 
   * @param {UpdateEvidenceDefinition} updateEvidenceDefinition 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.updateEvidenceDefinition('00000000-0000-0000-0000-000000000000', null);
   * ```
   * @openapi operation=updateEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=PUT
   * @memberof EvidenceDefinitionApi
   */
  updateEvidenceDefinition(id: UUID, updateEvidenceDefinition: UpdateEvidenceDefinition): Promise<EvidenceDefinitionWithScfLinksAndTags>;

}

/**
 * EvidenceDefinitionProducerApi - interface
 * Producer interface for EvidenceDefinition
 * @export
 * @interface EvidenceDefinitionProducerApi
 */
export interface EvidenceDefinitionProducerApi {
  /**
  * Creates a new evidence definition
  * @summary Creates a new evidence definition
  * @param {NewEvidenceDefinition} newEvidenceDefinition 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=createEvidenceDefinition path=/catalog/evidenceDefinitions method=POST
  * @memberof EvidenceDefinition
  */
  createEvidenceDefinition(newEvidenceDefinition: NewEvidenceDefinition): Promise<EvidenceDefinitionWithScfLinksAndTags>;
  /**
  * Delete evidence definition
  * @summary Delete evidence definition
  * @param {UUID} id 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=deleteEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=DELETE
  * @memberof EvidenceDefinition
  */
  deleteEvidenceDefinition(id: UUID): Promise<void>;
  /**
  * Get an evidence definition
  * @summary Get an evidence definition
  * @param {UUID} id 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=GET
  * @memberof EvidenceDefinition
  */
  getEvidenceDefinition(id: UUID): Promise<EvidenceDefinitionWithScfLinksAndTags>;
  /**
  * List framework elements that have links to evidence definition
  * @summary List framework elements that have links to evidence definition
  * @param {UUID} id 
  * @param {UUID} [frameworkVersionId] Filter by framework version
  * @param {EvidenceDefinitionScfLinkScope} [scope] Filter by scope
  * @param {EvidenceDefinitionScfLinkRequired} [required] Filter by required
  * @param {SortObject} [sort] Sort object
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEvidenceDefinitionFrameworkElements path=/catalog/evidenceDefinitions/{id}/frameworkElements method=GET
  * @memberof EvidenceDefinition
  */
  listEvidenceDefinitionFrameworkElements(results: PagedResults<EvidenceDefinitionFrameworkElementLink>, id: UUID, frameworkVersionId?: UUID, scope?: EvidenceDefinitionScfLinkScopeDef, required?: EvidenceDefinitionScfLinkRequiredDef, sort?: SortObject): Promise<void>;
  /**
  * List framework version that have links to evidence definition
  * @summary List framework version that have links to evidence definition
  * @param {UUID} id 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEvidenceDefinitionFrameworkVersions path=/catalog/evidenceDefinitions/{id}/frameworkVersions method=GET
  * @memberof EvidenceDefinition
  */
  listEvidenceDefinitionFrameworkVersions(results: PagedResults<EvidenceDefinitionFrameworkVersion>, id: UUID): Promise<void>;
  /**
  * List evidence definitions
  * @summary List evidence definitions
  * @param {string} [name] Filter by name
  * @param {PublishEvidenceStatusEnum} [status] Filter by status
  * @param {string} [sort] 
  * @param {UUID} [internalControlId] 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEvidenceDefinitions path=/catalog/evidenceDefinitions method=GET
  * @memberof EvidenceDefinition
  */
  listEvidenceDefinitions(results: PagedResults<EvidenceDefinitionWithScfLinksAndTags>, name?: string, status?: PublishEvidenceStatusEnumDef, sort?: string, internalControlId?: UUID): Promise<void>;
  /**
  * Returns options/filters for evidence definitions
  * @summary Returns options/filters for evidence definitions
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchEvidenceDefinitionOptions path=/evidenceDefinitionSearch/options method=GET
  * @memberof EvidenceDefinition
  */
  searchOptions(): Promise<ResultsColumnOptionsWrapper>;
  /**
  * Search all evidence definitions
  * @summary Search all evidence definitions
  * @param {SearchEvidenceDefinitionBody} searchEvidenceDefinitionBody 
  * @param {SortObject} [sort] 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchEvidenceDefinitions path=/evidenceDefinitionSearch method=POST
  * @memberof EvidenceDefinition
  */
  search(results: PagedResults<EvidenceDefinitionExtended>, searchEvidenceDefinitionBody: SearchEvidenceDefinitionBody, sort?: SortObject): Promise<void>;
  /**
  * Update evidence definition
  * @summary Update evidence definition
  * @param {UUID} id 
  * @param {UpdateEvidenceDefinition} updateEvidenceDefinition 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=updateEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=PUT
  * @memberof EvidenceDefinition
  */
  updateEvidenceDefinition(id: UUID, updateEvidenceDefinition: UpdateEvidenceDefinition): Promise<EvidenceDefinitionWithScfLinksAndTags>;
}

export class EvidenceDefinitionApiMappingImpl implements EvidenceDefinitionApi {
  private producer: EvidenceDefinitionProducerApi;
  constructor(producer: EvidenceDefinitionProducerApi) {
    this.producer = producer;
  }

  /**
   * Creates a new evidence definition
   * @summary Creates a new evidence definition
   * @param newEvidenceDefinition 
   * @openapi operation=createEvidenceDefinition path=/catalog/evidenceDefinitions method=POST
   */
  public async createEvidenceDefinition(newEvidenceDefinition: NewEvidenceDefinition): Promise<EvidenceDefinitionWithScfLinksAndTags> {
    // verify required parameter 'newEvidenceDefinition' is not null or undefined
    if (newEvidenceDefinition === null || newEvidenceDefinition === undefined) {
      throw new ParameterRequiredError('newEvidenceDefinition');
    }
    return this.producer.createEvidenceDefinition(newEvidenceDefinition);
  }
  /**
   * Delete evidence definition
   * @summary Delete evidence definition
   * @param id 
   * @openapi operation=deleteEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=DELETE
   */
  public async deleteEvidenceDefinition(id: UUID): Promise<void> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.deleteEvidenceDefinition(id);
  }
  /**
   * Get an evidence definition
   * @summary Get an evidence definition
   * @param id 
   * @openapi operation=getEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=GET
   */
  public async getEvidenceDefinition(id: UUID): Promise<EvidenceDefinitionWithScfLinksAndTags> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.getEvidenceDefinition(id);
  }
  /**
   * List framework elements that have links to evidence definition
   * @summary List framework elements that have links to evidence definition
   * @param id 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param frameworkVersionId Filter by framework version
   * @param scope Filter by scope
   * @param required Filter by required
   * @param sort Sort object
   * @param pageToken Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @openapi operation=listEvidenceDefinitionFrameworkElements path=/catalog/evidenceDefinitions/{id}/frameworkElements method=GET
   */
  public async listEvidenceDefinitionFrameworkElements(id: UUID, pageNumber: number = 1, pageSize: number = 50, frameworkVersionId?: UUID, scope?: EvidenceDefinitionScfLinkScopeDef, required?: EvidenceDefinitionScfLinkRequiredDef, sort?: SortObject, pageToken?: string): Promise<PagedResults<EvidenceDefinitionFrameworkElementLink>> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EvidenceDefinitionFrameworkElementLink>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    bag.pageToken = pageToken;
    await this.producer.listEvidenceDefinitionFrameworkElements(bag, id, frameworkVersionId, scope, required, sort);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * List framework version that have links to evidence definition
   * @summary List framework version that have links to evidence definition
   * @param id 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param pageToken Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @openapi operation=listEvidenceDefinitionFrameworkVersions path=/catalog/evidenceDefinitions/{id}/frameworkVersions method=GET
   */
  public async listEvidenceDefinitionFrameworkVersions(id: UUID, pageNumber: number = 1, pageSize: number = 50, pageToken?: string): Promise<PagedResults<EvidenceDefinitionFrameworkVersion>> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EvidenceDefinitionFrameworkVersion>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    bag.pageToken = pageToken;
    await this.producer.listEvidenceDefinitionFrameworkVersions(bag, id);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * List evidence definitions
   * @summary List evidence definitions
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param name Filter by name
   * @param status Filter by status
   * @param sort 
   * @param internalControlId 
   * @param pageToken Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @openapi operation=listEvidenceDefinitions path=/catalog/evidenceDefinitions method=GET
   */
  public async listEvidenceDefinitions(pageNumber: number = 1, pageSize: number = 50, name?: string, status?: PublishEvidenceStatusEnumDef, sort?: string, internalControlId?: UUID, pageToken?: string): Promise<PagedResults<EvidenceDefinitionWithScfLinksAndTags>> {
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EvidenceDefinitionWithScfLinksAndTags>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    bag.pageToken = pageToken;
    await this.producer.listEvidenceDefinitions(bag, name, status, sort, internalControlId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * Returns options/filters for evidence definitions
   * @summary Returns options/filters for evidence definitions
   * @openapi operation=searchEvidenceDefinitionOptions path=/evidenceDefinitionSearch/options method=GET
   */
  public async searchOptions(): Promise<ResultsColumnOptionsWrapper> {
    return this.producer.searchOptions();
  }
  /**
   * Search all evidence definitions
   * @summary Search all evidence definitions
   * @param searchEvidenceDefinitionBody 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sort 
   * @param pageToken Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @openapi operation=searchEvidenceDefinitions path=/evidenceDefinitionSearch method=POST
   */
  public async search(searchEvidenceDefinitionBody: SearchEvidenceDefinitionBody, pageNumber: number = 1, pageSize: number = 50, sort?: SortObject, pageToken?: string): Promise<PagedResults<EvidenceDefinitionExtended>> {
    // verify required parameter 'searchEvidenceDefinitionBody' is not null or undefined
    if (searchEvidenceDefinitionBody === null || searchEvidenceDefinitionBody === undefined) {
      throw new ParameterRequiredError('searchEvidenceDefinitionBody');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<EvidenceDefinitionExtended>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    bag.pageToken = pageToken;
    await this.producer.search(bag, searchEvidenceDefinitionBody, sort);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * Update evidence definition
   * @summary Update evidence definition
   * @param id 
   * @param updateEvidenceDefinition 
   * @openapi operation=updateEvidenceDefinition path=/catalog/evidenceDefinitions/{id} method=PUT
   */
  public async updateEvidenceDefinition(id: UUID, updateEvidenceDefinition: UpdateEvidenceDefinition): Promise<EvidenceDefinitionWithScfLinksAndTags> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    // verify required parameter 'updateEvidenceDefinition' is not null or undefined
    if (updateEvidenceDefinition === null || updateEvidenceDefinition === undefined) {
      throw new ParameterRequiredError('updateEvidenceDefinition');
    }
    return this.producer.updateEvidenceDefinition(id, updateEvidenceDefinition);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapEvidenceDefinitionProducer(producer: EvidenceDefinitionProducerApi): EvidenceDefinitionApi {
  return new EvidenceDefinitionApiMappingImpl(producer);
}

/**
 * EvidenceDefinitionApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class EvidenceDefinitionApiHttpImpl implements EvidenceDefinitionProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async createEvidenceDefinition(newEvidenceDefinition: NewEvidenceDefinition): Promise<EvidenceDefinitionWithScfLinksAndTags> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      ...(newEvidenceDefinition ?? {}),
  };
  let _path = '/catalog/evidenceDefinitions';
  return this.client.post(
    _path,
    _bodyParams, { params: _queryParams, paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }),  })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'EvidenceDefinitionWithScfLinksAndTags')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async deleteEvidenceDefinition(id: UUID): Promise<void> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/catalog/evidenceDefinitions/{id}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.delete(
    _path,
    { params: _queryParams, paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }) })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, '')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async getEvidenceDefinition(id: UUID): Promise<EvidenceDefinitionWithScfLinksAndTags> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/catalog/evidenceDefinitions/{id}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    { params: _queryParams, paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }) })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'EvidenceDefinitionWithScfLinksAndTags')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listEvidenceDefinitionFrameworkElements(results: PagedResults<EvidenceDefinitionFrameworkElementLink>, id: UUID, frameworkVersionId?: UUID, scope?: EvidenceDefinitionScfLinkScopeDef, required?: EvidenceDefinitionScfLinkRequiredDef, sort?: SortObject): Promise<void> {
  const _queryParams = {
      frameworkVersionId,scope,required,sort,
  };
  const _bodyParams = {
      
  };
  const _body = {
      id,frameworkVersionId,scope,required,sort,
  };
  let _path = '/catalog/evidenceDefinitions/{id}/frameworkElements';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,pageToken: results.pageToken,
        pageSize: results.pageSize,
      },
      paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }),
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EvidenceDefinitionFrameworkElementLink>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}${_path}`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EvidenceDefinitionFrameworkElementLink');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listEvidenceDefinitionFrameworkVersions(results: PagedResults<EvidenceDefinitionFrameworkVersion>, id: UUID): Promise<void> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  const _body = {
      id,
  };
  let _path = '/catalog/evidenceDefinitions/{id}/frameworkVersions';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,pageToken: results.pageToken,
        pageSize: results.pageSize,
      },
      paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }),
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EvidenceDefinitionFrameworkVersion>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}${_path}`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EvidenceDefinitionFrameworkVersion');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listEvidenceDefinitions(results: PagedResults<EvidenceDefinitionWithScfLinksAndTags>, name?: string, status?: PublishEvidenceStatusEnumDef, sort?: string, internalControlId?: UUID): Promise<void> {
  const _queryParams = {
      name,status,sort,internalControlId,
  };
  const _bodyParams = {
      
  };
  const _body = {
      name,status,sort,internalControlId,
  };
  let _path = '/catalog/evidenceDefinitions';
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,pageToken: results.pageToken,
        pageSize: results.pageSize,
      },
      paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }),
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EvidenceDefinitionWithScfLinksAndTags>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}${_path}`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EvidenceDefinitionWithScfLinksAndTags');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async searchOptions(): Promise<ResultsColumnOptionsWrapper> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/evidenceDefinitionSearch/options';
  return this.client.get(
    _path,
    { params: _queryParams, paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }) })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'ResultsColumnOptionsWrapper')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async search(results: PagedResults<EvidenceDefinitionExtended>, searchEvidenceDefinitionBody: SearchEvidenceDefinitionBody, sort?: SortObject): Promise<void> {
  const _queryParams = {
      sort,
  };
  const _bodyParams = {
      ...(searchEvidenceDefinitionBody ?? {}),
  };
  const _body = {
      ...(searchEvidenceDefinitionBody ?? {}),
  };
  let _path = '/evidenceDefinitionSearch';
  return this.client.post(
    _path,
    _bodyParams,
    {
      params: {
        sort,
        pageNumber: results.pageNumber,pageToken: results.pageToken,
        pageSize: results.pageSize,
      },
      paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }),
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<EvidenceDefinitionExtended>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}${_path}`);
      results.httpMethod = HttpMethod.Post;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'EvidenceDefinitionExtended');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async updateEvidenceDefinition(id: UUID, updateEvidenceDefinition: UpdateEvidenceDefinition): Promise<EvidenceDefinitionWithScfLinksAndTags> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      ...(updateEvidenceDefinition ?? {}),
  };
  let _path = '/catalog/evidenceDefinitions/{id}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.put(
    _path,
    _bodyParams, { params: _queryParams, paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }),  })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'EvidenceDefinitionWithScfLinksAndTags')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

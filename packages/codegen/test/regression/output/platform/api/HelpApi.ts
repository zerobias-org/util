/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { HelpCase } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace HelpApi {
}

/**
 * HelpApi - interface
 * @export
 * @interface HelpApi
 */
export interface HelpApi {
  /**
   * Creates a new help case on Salesforce
   * @summary Creates a new help case on Salesforce
   * @param {HelpCase} helpCase 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.createHelpCase(null);
   * ```
   * @openapi operation=createHelpCase path=/help/cases method=POST
   * @memberof HelpApi
   */
  createHelpCase(helpCase: HelpCase): Promise<void>;

}

/**
 * HelpProducerApi - interface
 * Producer interface for Help
 * @export
 * @interface HelpProducerApi
 */
export interface HelpProducerApi {
  /**
  * Creates a new help case on Salesforce
  * @summary Creates a new help case on Salesforce
  * @param {HelpCase} helpCase 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=createHelpCase path=/help/cases method=POST
  * @memberof Help
  */
  createHelpCase(helpCase: HelpCase): Promise<void>;
}

export class HelpApiMappingImpl implements HelpApi {
  private producer: HelpProducerApi;
  constructor(producer: HelpProducerApi) {
    this.producer = producer;
  }

  /**
   * Creates a new help case on Salesforce
   * @summary Creates a new help case on Salesforce
   * @param helpCase 
   * @openapi operation=createHelpCase path=/help/cases method=POST
   */
  public async createHelpCase(helpCase: HelpCase): Promise<void> {
    // verify required parameter 'helpCase' is not null or undefined
    if (helpCase === null || helpCase === undefined) {
      throw new ParameterRequiredError('helpCase');
    }
    return this.producer.createHelpCase(helpCase);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapHelpProducer(producer: HelpProducerApi): HelpApi {
  return new HelpApiMappingImpl(producer);
}

/**
 * HelpApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class HelpApiHttpImpl implements HelpProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async createHelpCase(helpCase: HelpCase): Promise<void> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      ...helpCase,
  };
  let _path = '/help/cases';
  return this.client.post(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, '')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

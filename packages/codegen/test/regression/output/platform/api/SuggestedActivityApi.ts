/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { SuggestedActivityExtended } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace SuggestedActivityApi {
}

/**
 * SuggestedActivityApi - interface
 * @export
 * @interface SuggestedActivityApi
 */
export interface SuggestedActivityApi {
  /**
   * Get suggested activity by id or code
   * @summary Get suggested activity by id or code
   * @param {string} idOrCode 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.get('idOrCode');
   * ```
   * @openapi operation=getSuggestedActivity path=/catalog/suggestedActivities/{idOrCode} method=GET
   * @memberof SuggestedActivityApi
   */
  get(idOrCode: string): Promise<SuggestedActivityExtended>;

  /**
   * list all suggested activities
   * @summary list all suggested activities
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {string} [scfControlIdOrCode] 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list(1, 1, 'scfControlIdOrCode');
   * ```
   * @openapi operation=listSuggestedActivities path=/catalog/suggestedActivities method=GET
   * @memberof SuggestedActivityApi
   */
  list(pageNumber?: number, pageSize?: number, scfControlIdOrCode?: string): Promise<PagedResults<SuggestedActivityExtended>>;

}

/**
 * SuggestedActivityProducerApi - interface
 * Producer interface for SuggestedActivity
 * @export
 * @interface SuggestedActivityProducerApi
 */
export interface SuggestedActivityProducerApi {
  /**
  * Get suggested activity by id or code
  * @summary Get suggested activity by id or code
  * @param {string} idOrCode 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getSuggestedActivity path=/catalog/suggestedActivities/{idOrCode} method=GET
  * @memberof SuggestedActivity
  */
  get(idOrCode: string): Promise<SuggestedActivityExtended>;
  /**
  * list all suggested activities
  * @summary list all suggested activities
  * @param {string} [scfControlIdOrCode] 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listSuggestedActivities path=/catalog/suggestedActivities method=GET
  * @memberof SuggestedActivity
  */
  list(results: PagedResults<SuggestedActivityExtended>, scfControlIdOrCode?: string): Promise<void>;
}

export class SuggestedActivityApiMappingImpl implements SuggestedActivityApi {
  private producer: SuggestedActivityProducerApi;
  constructor(producer: SuggestedActivityProducerApi) {
    this.producer = producer;
  }

  /**
   * Get suggested activity by id or code
   * @summary Get suggested activity by id or code
   * @param idOrCode 
   * @openapi operation=getSuggestedActivity path=/catalog/suggestedActivities/{idOrCode} method=GET
   */
  public async get(idOrCode: string): Promise<SuggestedActivityExtended> {
    // verify required parameter 'idOrCode' is not null or undefined
    if (idOrCode === null || idOrCode === undefined || idOrCode === '') {
      throw new ParameterRequiredError('idOrCode');
    }
    return this.producer.get(idOrCode);
  }
  /**
   * list all suggested activities
   * @summary list all suggested activities
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param scfControlIdOrCode 
   * @openapi operation=listSuggestedActivities path=/catalog/suggestedActivities method=GET
   */
  public async list(pageNumber: number = 1, pageSize: number = 50, scfControlIdOrCode?: string): Promise<PagedResults<SuggestedActivityExtended>> {
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<SuggestedActivityExtended>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.list(bag, scfControlIdOrCode);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapSuggestedActivityProducer(producer: SuggestedActivityProducerApi): SuggestedActivityApi {
  return new SuggestedActivityApiMappingImpl(producer);
}

/**
 * SuggestedActivityApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class SuggestedActivityApiHttpImpl implements SuggestedActivityProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async get(idOrCode: string): Promise<SuggestedActivityExtended> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/catalog/suggestedActivities/{idOrCode}';
  _path = _path.replace('{idOrCode}', encodeURIComponent(String(idOrCode)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'SuggestedActivityExtended')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(results: PagedResults<SuggestedActivityExtended>, scfControlIdOrCode?: string): Promise<void> {
  const _body = {
      scfControlIdOrCode,
  };
  let _path = '/catalog/suggestedActivities';
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<SuggestedActivityExtended>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/catalog/suggestedActivities`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'SuggestedActivityExtended');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

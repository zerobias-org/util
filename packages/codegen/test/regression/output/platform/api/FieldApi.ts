/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { ExtendedField, ExtendedProperty, Field, Property } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace FieldApi {
}

/**
 * FieldApi - interface
 * @export
 * @interface FieldApi
 */
export interface FieldApi {
  /**
   * Retrieves a field info by id
   * @summary Retrieves a field info by id
   * @param {UUID} fieldId 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.getField('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=getField path=/catalog/fields/{fieldId} method=GET
   * @memberof FieldApi
   */
  getField(fieldId: UUID): Promise<Field & ExtendedField>;

  /**
   * Retrieves a fields properties
   * @summary Retrieves a fields properties
   * @param {UUID} fieldId 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listFieldProperties('00000000-0000-0000-0000-000000000000', 1, 1);
   * ```
   * @openapi operation=listFieldProperties path=/catalog/fields/{fieldId}/properties method=GET
   * @memberof FieldApi
   */
  listFieldProperties(fieldId: UUID, pageNumber?: number, pageSize?: number): Promise<PagedResults<Property & ExtendedProperty>>;

  /**
   * Retrieves a list of all fields with searchable queries
   * @summary Retrieves a list of all fields with searchable queries
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {UUID} [dataTypeId] Search for fields by data type id
   * @param {string} [search] An string to search for
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listFields(1, 1, '00000000-0000-0000-0000-000000000000', 'search');
   * ```
   * @openapi operation=listFields path=/catalog/fields method=GET
   * @memberof FieldApi
   */
  listFields(pageNumber?: number, pageSize?: number, dataTypeId?: UUID, search?: string): Promise<PagedResults<Field & ExtendedField>>;

}

/**
 * FieldProducerApi - interface
 * Producer interface for Field
 * @export
 * @interface FieldProducerApi
 */
export interface FieldProducerApi {
  /**
  * Retrieves a field info by id
  * @summary Retrieves a field info by id
  * @param {UUID} fieldId 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getField path=/catalog/fields/{fieldId} method=GET
  * @memberof Field
  */
  getField(fieldId: UUID): Promise<Field & ExtendedField>;
  /**
  * Retrieves a fields properties
  * @summary Retrieves a fields properties
  * @param {UUID} fieldId 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listFieldProperties path=/catalog/fields/{fieldId}/properties method=GET
  * @memberof Field
  */
  listFieldProperties(results: PagedResults<Property & ExtendedProperty>, fieldId: UUID): Promise<void>;
  /**
  * Retrieves a list of all fields with searchable queries
  * @summary Retrieves a list of all fields with searchable queries
  * @param {UUID} [dataTypeId] Search for fields by data type id
  * @param {string} [search] An string to search for
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listFields path=/catalog/fields method=GET
  * @memberof Field
  */
  listFields(results: PagedResults<Field & ExtendedField>, dataTypeId?: UUID, search?: string): Promise<void>;
}

export class FieldApiMappingImpl implements FieldApi {
  private producer: FieldProducerApi;
  constructor(producer: FieldProducerApi) {
    this.producer = producer;
  }

  /**
   * Retrieves a field info by id
   * @summary Retrieves a field info by id
   * @param fieldId 
   * @openapi operation=getField path=/catalog/fields/{fieldId} method=GET
   */
  public async getField(fieldId: UUID): Promise<Field & ExtendedField> {
    // verify required parameter 'fieldId' is not null or undefined
    if (fieldId === null || fieldId === undefined) {
      throw new ParameterRequiredError('fieldId');
    }
    return this.producer.getField(fieldId);
  }
  /**
   * Retrieves a fields properties
   * @summary Retrieves a fields properties
   * @param fieldId 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listFieldProperties path=/catalog/fields/{fieldId}/properties method=GET
   */
  public async listFieldProperties(fieldId: UUID, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<Property & ExtendedProperty>> {
    // verify required parameter 'fieldId' is not null or undefined
    if (fieldId === null || fieldId === undefined) {
      throw new ParameterRequiredError('fieldId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Property & ExtendedProperty>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listFieldProperties(bag, fieldId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * Retrieves a list of all fields with searchable queries
   * @summary Retrieves a list of all fields with searchable queries
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param dataTypeId Search for fields by data type id
   * @param search An string to search for
   * @openapi operation=listFields path=/catalog/fields method=GET
   */
  public async listFields(pageNumber: number = 1, pageSize: number = 50, dataTypeId?: UUID, search?: string): Promise<PagedResults<Field & ExtendedField>> {
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Field & ExtendedField>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listFields(bag, dataTypeId, search);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapFieldProducer(producer: FieldProducerApi): FieldApi {
  return new FieldApiMappingImpl(producer);
}

/**
 * FieldApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class FieldApiHttpImpl implements FieldProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async getField(fieldId: UUID): Promise<Field & ExtendedField> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/catalog/fields/{fieldId}';
  _path = _path.replace('{fieldId}', encodeURIComponent(String(fieldId)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Field & ExtendedField')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listFieldProperties(results: PagedResults<Property & ExtendedProperty>, fieldId: UUID): Promise<void> {
  const _body = {
      fieldId,
  };
  let _path = '/catalog/fields/{fieldId}/properties';
  _path = _path.replace('{fieldId}', encodeURIComponent(String(fieldId)));
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Property & ExtendedProperty>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/catalog/fields/{fieldId}/properties`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Property & ExtendedProperty');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async listFields(results: PagedResults<Field & ExtendedField>, dataTypeId?: UUID, search?: string): Promise<void> {
  const _body = {
      dataTypeId,search,
  };
  let _path = '/catalog/fields';
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Field & ExtendedField>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/catalog/fields`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Field & ExtendedField');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

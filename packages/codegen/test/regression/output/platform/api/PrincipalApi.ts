/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import qs from 'qs';
import { PrincipalAccessRuleGroupedView, PrincipalAccessRuleSearchBody, ResultsColumnOptionsWrapper } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

// Helper to serialize core types (UUID, Nmtoken, URL, etc.) and EnumValues for query params
// All @zerobias-org/types-core-js types implement toJSON() for proper serialization
function serializeQueryParams(obj: any): any {
  if (obj === null || obj === undefined) return obj;
  if (typeof obj !== 'object') return obj;
  if (Array.isArray(obj)) return obj.map(serializeQueryParams);
  // Core types and EnumValues all have toJSON() - use it for serialization
  if (typeof obj.toJSON === 'function') {
    return obj.toJSON();
  }
  // Recursively process plain object properties
  const result: any = {};
  for (const key of Object.keys(obj)) {
    result[key] = serializeQueryParams(obj[key]);
  }
  return result;
}

export namespace PrincipalApi {
}

/**
 * PrincipalApi - interface
 * @export
 * @interface PrincipalApi
 */
export interface PrincipalApi {
  /**
   * 
   * @summary Retrieves options for access rules search for a principal.
   * @param {UUID} id ID of the principal to filter access rules with
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.principalAccessRuleOptions('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=principalAccessRuleOptions path=/principals/{id}/accessRuleSearch/options method=GET
   * @memberof PrincipalApi
   */
  principalAccessRuleOptions(id: UUID): Promise<ResultsColumnOptionsWrapper>;

  /**
   * 
   * @summary Searches for access rules that belong to a principal (directly or indirectly through role assignments).
   * @param {UUID} id ID of the principal to filter access rules with
   * @param {PrincipalAccessRuleSearchBody} principalAccessRuleSearchBody 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {object} [sort] Sortable object
   * @param {string} [pageToken] Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.searchPrincipalAccessRules('00000000-0000-0000-0000-000000000000', null, 1, 1, null, 'pageToken');
   * ```
   * @openapi operation=searchPrincipalAccessRules path=/principals/{id}/accessRuleSearch method=POST
   * @memberof PrincipalApi
   */
  searchPrincipalAccessRules(id: UUID, principalAccessRuleSearchBody: PrincipalAccessRuleSearchBody, pageNumber?: number, pageSize?: number, sort?: object, pageToken?: string): Promise<PagedResults<PrincipalAccessRuleGroupedView>>;

}

/**
 * PrincipalProducerApi - interface
 * Producer interface for Principal
 * @export
 * @interface PrincipalProducerApi
 */
export interface PrincipalProducerApi {
  /**
  * 
  * @summary Retrieves options for access rules search for a principal.
  * @param {UUID} id ID of the principal to filter access rules with
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=principalAccessRuleOptions path=/principals/{id}/accessRuleSearch/options method=GET
  * @memberof Principal
  */
  principalAccessRuleOptions(id: UUID): Promise<ResultsColumnOptionsWrapper>;
  /**
  * 
  * @summary Searches for access rules that belong to a principal (directly or indirectly through role assignments).
  * @param {UUID} id ID of the principal to filter access rules with
  * @param {PrincipalAccessRuleSearchBody} principalAccessRuleSearchBody 
  * @param {object} [sort] Sortable object
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchPrincipalAccessRules path=/principals/{id}/accessRuleSearch method=POST
  * @memberof Principal
  */
  searchPrincipalAccessRules(results: PagedResults<PrincipalAccessRuleGroupedView>, id: UUID, principalAccessRuleSearchBody: PrincipalAccessRuleSearchBody, sort?: object): Promise<void>;
}

export class PrincipalApiMappingImpl implements PrincipalApi {
  private producer: PrincipalProducerApi;
  constructor(producer: PrincipalProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves options for access rules search for a principal.
   * @param id ID of the principal to filter access rules with
   * @openapi operation=principalAccessRuleOptions path=/principals/{id}/accessRuleSearch/options method=GET
   */
  public async principalAccessRuleOptions(id: UUID): Promise<ResultsColumnOptionsWrapper> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.principalAccessRuleOptions(id);
  }
  /**
   * 
   * @summary Searches for access rules that belong to a principal (directly or indirectly through role assignments).
   * @param id ID of the principal to filter access rules with
   * @param principalAccessRuleSearchBody 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sort Sortable object
   * @param pageToken Opaque cursor for cursor-based pagination. When provided, pageNumber is ignored. Use the token from the previous response&#39;s pageToken header to get the next page.
   * @openapi operation=searchPrincipalAccessRules path=/principals/{id}/accessRuleSearch method=POST
   */
  public async searchPrincipalAccessRules(id: UUID, principalAccessRuleSearchBody: PrincipalAccessRuleSearchBody, pageNumber: number = 1, pageSize: number = 50, sort?: object, pageToken?: string): Promise<PagedResults<PrincipalAccessRuleGroupedView>> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    // verify required parameter 'principalAccessRuleSearchBody' is not null or undefined
    if (principalAccessRuleSearchBody === null || principalAccessRuleSearchBody === undefined) {
      throw new ParameterRequiredError('principalAccessRuleSearchBody');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<PrincipalAccessRuleGroupedView>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    bag.pageToken = pageToken;
    await this.producer.searchPrincipalAccessRules(bag, id, principalAccessRuleSearchBody, sort);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapPrincipalProducer(producer: PrincipalProducerApi): PrincipalApi {
  return new PrincipalApiMappingImpl(producer);
}

/**
 * PrincipalApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class PrincipalApiHttpImpl implements PrincipalProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async principalAccessRuleOptions(id: UUID): Promise<ResultsColumnOptionsWrapper> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/principals/{id}/accessRuleSearch/options';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    { params: _queryParams, paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }) })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'ResultsColumnOptionsWrapper')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async searchPrincipalAccessRules(results: PagedResults<PrincipalAccessRuleGroupedView>, id: UUID, principalAccessRuleSearchBody: PrincipalAccessRuleSearchBody, sort?: object): Promise<void> {
  const _queryParams = {
      sort,
  };
  const _bodyParams = {
      ...(principalAccessRuleSearchBody ?? {}),
  };
  const _body = {
      ...(principalAccessRuleSearchBody ?? {}),
  };
  let _path = '/principals/{id}/accessRuleSearch';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.post(
    _path,
    _bodyParams,
    {
      params: {
        sort,
        pageNumber: results.pageNumber,pageToken: results.pageToken,
        pageSize: results.pageSize,
      },
      paramsSerializer: (params) => qs.stringify(serializeQueryParams(params), { skipNulls: true }),
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<PrincipalAccessRuleGroupedView>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}${_path}`);
      results.httpMethod = HttpMethod.Post;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'PrincipalAccessRuleGroupedView');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

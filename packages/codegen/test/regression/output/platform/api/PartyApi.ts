/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { Party, PartyTypeEnumDef, SortObject } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace PartyApi {
}

/**
 * PartyApi - interface
 * @export
 * @interface PartyApi
 */
export interface PartyApi {
  /**
   * Get party for the current principal in the current organization
   * @summary Get party for the current principal in the current organization
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @openapi operation=getMyParty path=/app/party/me method=GET
   * @memberof PartyApi
   */
  getMyParty(): Promise<Party>;

  /**
   * Get party by id, a principal id may also be provided to get the party for that principal in the current logged on organization
   * @summary Get party by id, a principal id may also be provided to get the party for that principal in the current logged on organization
   * @param {UUID} id 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.get('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=getParty path=/app/parties/{id} method=GET
   * @memberof PartyApi
   */
  get(id: UUID): Promise<Party>;

  /**
   * list all parties
   * @summary list all parties
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {SortObject} [sort] Sort object
   * @param {boolean} [assignable] 
   * @param {string} [search] 
   * @param {PartyTypeEnum} [partyType] 
   * @param {UUID} [boundaryId] 
   * @param {string} [roleIds] A comma serparated list of role ids. Filter by role ids. Currenly, this will only return assignable parties.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list(1, 1, null, true, 'search', null, '00000000-0000-0000-0000-000000000000', 'roleIds');
   * ```
   * @openapi operation=listParties path=/app/parties method=GET
   * @memberof PartyApi
   */
  list(pageNumber?: number, pageSize?: number, sort?: SortObject, assignable?: boolean, search?: string, partyType?: PartyTypeEnumDef, boundaryId?: UUID, roleIds?: string): Promise<PagedResults<Party>>;

}

/**
 * PartyProducerApi - interface
 * Producer interface for Party
 * @export
 * @interface PartyProducerApi
 */
export interface PartyProducerApi {
  /**
  * Get party for the current principal in the current organization
  * @summary Get party for the current principal in the current organization
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getMyParty path=/app/party/me method=GET
  * @memberof Party
  */
  getMyParty(): Promise<Party>;
  /**
  * Get party by id, a principal id may also be provided to get the party for that principal in the current logged on organization
  * @summary Get party by id, a principal id may also be provided to get the party for that principal in the current logged on organization
  * @param {UUID} id 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getParty path=/app/parties/{id} method=GET
  * @memberof Party
  */
  get(id: UUID): Promise<Party>;
  /**
  * list all parties
  * @summary list all parties
  * @param {SortObject} [sort] Sort object
  * @param {boolean} [assignable] 
  * @param {string} [search] 
  * @param {PartyTypeEnum} [partyType] 
  * @param {UUID} [boundaryId] 
  * @param {string} [roleIds] A comma serparated list of role ids. Filter by role ids. Currenly, this will only return assignable parties.
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listParties path=/app/parties method=GET
  * @memberof Party
  */
  list(results: PagedResults<Party>, sort?: SortObject, assignable?: boolean, search?: string, partyType?: PartyTypeEnumDef, boundaryId?: UUID, roleIds?: string): Promise<void>;
}

export class PartyApiMappingImpl implements PartyApi {
  private producer: PartyProducerApi;
  constructor(producer: PartyProducerApi) {
    this.producer = producer;
  }

  /**
   * Get party for the current principal in the current organization
   * @summary Get party for the current principal in the current organization
   * @openapi operation=getMyParty path=/app/party/me method=GET
   */
  public async getMyParty(): Promise<Party> {
    return this.producer.getMyParty();
  }
  /**
   * Get party by id, a principal id may also be provided to get the party for that principal in the current logged on organization
   * @summary Get party by id, a principal id may also be provided to get the party for that principal in the current logged on organization
   * @param id 
   * @openapi operation=getParty path=/app/parties/{id} method=GET
   */
  public async get(id: UUID): Promise<Party> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.get(id);
  }
  /**
   * list all parties
   * @summary list all parties
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sort Sort object
   * @param assignable 
   * @param search 
   * @param partyType 
   * @param boundaryId 
   * @param roleIds A comma serparated list of role ids. Filter by role ids. Currenly, this will only return assignable parties.
   * @openapi operation=listParties path=/app/parties method=GET
   */
  public async list(pageNumber: number = 1, pageSize: number = 50, sort?: SortObject, assignable?: boolean, search?: string, partyType?: PartyTypeEnumDef, boundaryId?: UUID, roleIds?: string): Promise<PagedResults<Party>> {
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Party>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.list(bag, sort, assignable, search, partyType, boundaryId, roleIds);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapPartyProducer(producer: PartyProducerApi): PartyApi {
  return new PartyApiMappingImpl(producer);
}

/**
 * PartyApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class PartyApiHttpImpl implements PartyProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async getMyParty(): Promise<Party> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/app/party/me';
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Party')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async get(id: UUID): Promise<Party> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/app/parties/{id}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Party')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(results: PagedResults<Party>, sort?: SortObject, assignable?: boolean, search?: string, partyType?: PartyTypeEnumDef, boundaryId?: UUID, roleIds?: string): Promise<void> {
  const _body = {
      sort,assignable,search,partyType,boundaryId,roleIds,
  };
  let _path = '/app/parties';
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Party>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/app/parties`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Party');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

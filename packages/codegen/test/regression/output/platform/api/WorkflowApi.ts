/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { SortObject, Workflow, WorkflowExtended } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace WorkflowApi {
}

/**
 * WorkflowApi - interface
 * @export
 * @interface WorkflowApi
 */
export interface WorkflowApi {
  /**
   * Get workflow by id
   * @summary Get workflow by id
   * @param {UUID} id 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.get('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=getWorkflow path=/catalog/workflows/{id} method=GET
   * @memberof WorkflowApi
   */
  get(id: UUID): Promise<WorkflowExtended>;

  /**
   * list all workflows
   * @summary list all workflows
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {SortObject} [sort] Sort object
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list(1, 1, null);
   * ```
   * @openapi operation=listWorkflows path=/catalog/workflows method=GET
   * @memberof WorkflowApi
   */
  list(pageNumber?: number, pageSize?: number, sort?: SortObject): Promise<PagedResults<Workflow>>;

}

/**
 * WorkflowProducerApi - interface
 * Producer interface for Workflow
 * @export
 * @interface WorkflowProducerApi
 */
export interface WorkflowProducerApi {
  /**
  * Get workflow by id
  * @summary Get workflow by id
  * @param {UUID} id 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getWorkflow path=/catalog/workflows/{id} method=GET
  * @memberof Workflow
  */
  get(id: UUID): Promise<WorkflowExtended>;
  /**
  * list all workflows
  * @summary list all workflows
  * @param {SortObject} [sort] Sort object
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listWorkflows path=/catalog/workflows method=GET
  * @memberof Workflow
  */
  list(results: PagedResults<Workflow>, sort?: SortObject): Promise<void>;
}

export class WorkflowApiMappingImpl implements WorkflowApi {
  private producer: WorkflowProducerApi;
  constructor(producer: WorkflowProducerApi) {
    this.producer = producer;
  }

  /**
   * Get workflow by id
   * @summary Get workflow by id
   * @param id 
   * @openapi operation=getWorkflow path=/catalog/workflows/{id} method=GET
   */
  public async get(id: UUID): Promise<WorkflowExtended> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.get(id);
  }
  /**
   * list all workflows
   * @summary list all workflows
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sort Sort object
   * @openapi operation=listWorkflows path=/catalog/workflows method=GET
   */
  public async list(pageNumber: number = 1, pageSize: number = 50, sort?: SortObject): Promise<PagedResults<Workflow>> {
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Workflow>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.list(bag, sort);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapWorkflowProducer(producer: WorkflowProducerApi): WorkflowApi {
  return new WorkflowApiMappingImpl(producer);
}

/**
 * WorkflowApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class WorkflowApiHttpImpl implements WorkflowProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async get(id: UUID): Promise<WorkflowExtended> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/catalog/workflows/{id}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'WorkflowExtended')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(results: PagedResults<Workflow>, sort?: SortObject): Promise<void> {
  const _body = {
      sort,
  };
  let _path = '/catalog/workflows';
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Workflow>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/catalog/workflows`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Workflow');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

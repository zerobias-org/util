/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { EnumValue, IllegalArgumentError } from '@zerobias-org/types-core-js';
import { ObjectSerializer, RequestFile } from './index.js';

/**
* The type of the order, this may be product, catalog or operation.
*/
export const OrderType = {
  /** A low fidelity product order */
  Product: EnumValue.instance('OrderType', 'Product', 'product'),
  /** A cataloging order for a selected product */
  Catalog: EnumValue.instance('OrderType', 'Catalog', 'catalog'),
  /** An order for a set of operations in a given module */
  Operation: EnumValue.instance('OrderType', 'Operation', 'operation'),

  from(val: string | number): EnumValue {
    if (OrderType[val]) {
      return OrderType[val];
    }
    const byValue = OrderType.values.find((v) => v.value === val);
    if (byValue) {
      return byValue;
    }
    throw new IllegalArgumentError(`${val} is not a valid OrderType`);
  },

  get values(): EnumValue[] {
    return Object
      .keys(OrderType)
      .filter((k) => k !== 'values' && k !== 'from')
      .map((t) => OrderType[t]);
  }
} as const;

export type OrderTypeDef = typeof OrderType[keyof typeof OrderType];

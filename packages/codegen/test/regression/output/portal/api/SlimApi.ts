/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { IdNameDescriptionImageUrlObject } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace SlimApi {
}

/**
 * SlimApi - interface
 * @export
 * @interface SlimApi
 */
export interface SlimApi {
  /**
   * List all boundary products slim
   * @summary List all boundary products slim
   * @param {UUID} boundaryId 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listBoundaryProducts('00000000-0000-0000-0000-000000000000', 1, 1);
   * ```
   * @openapi operation=listSlimBoundaryProducts path=/slim/boundaries/{boundaryId}/boundaryProducts method=GET
   * @memberof SlimApi
   */
  listBoundaryProducts(boundaryId: UUID, pageNumber?: number, pageSize?: number): Promise<PagedResults<IdNameDescriptionImageUrlObject>>;

}

/**
 * SlimProducerApi - interface
 * Producer interface for Slim
 * @export
 * @interface SlimProducerApi
 */
export interface SlimProducerApi {
  /**
  * List all boundary products slim
  * @summary List all boundary products slim
  * @param {UUID} boundaryId 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listSlimBoundaryProducts path=/slim/boundaries/{boundaryId}/boundaryProducts method=GET
  * @memberof Slim
  */
  listBoundaryProducts(results: PagedResults<IdNameDescriptionImageUrlObject>, boundaryId: UUID): Promise<void>;
}

export class SlimApiMappingImpl implements SlimApi {
  private producer: SlimProducerApi;
  constructor(producer: SlimProducerApi) {
    this.producer = producer;
  }

  /**
   * List all boundary products slim
   * @summary List all boundary products slim
   * @param boundaryId 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @openapi operation=listSlimBoundaryProducts path=/slim/boundaries/{boundaryId}/boundaryProducts method=GET
   */
  public async listBoundaryProducts(boundaryId: UUID, pageNumber: number = 1, pageSize: number = 50): Promise<PagedResults<IdNameDescriptionImageUrlObject>> {
    // verify required parameter 'boundaryId' is not null or undefined
    if (boundaryId === null || boundaryId === undefined) {
      throw new ParameterRequiredError('boundaryId');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<IdNameDescriptionImageUrlObject>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.listBoundaryProducts(bag, boundaryId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapSlimProducer(producer: SlimProducerApi): SlimApi {
  return new SlimApiMappingImpl(producer);
}

/**
 * SlimApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class SlimApiHttpImpl implements SlimProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async listBoundaryProducts(results: PagedResults<IdNameDescriptionImageUrlObject>, boundaryId: UUID): Promise<void> {
  const _body = {
      boundaryId,
  };
  let _path = '/slim/boundaries/{boundaryId}/boundaryProducts';
  _path = _path.replace('{boundaryId}', encodeURIComponent(String(boundaryId)));
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<IdNameDescriptionImageUrlObject>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/slim/boundaries/{boundaryId}/boundaryProducts`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'IdNameDescriptionImageUrlObject');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

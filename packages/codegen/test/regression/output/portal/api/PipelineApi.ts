/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { PipelineExtended, ResultsColumnOptionsWrapper, SearchPipelineBody, SortObject } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace PipelineApi {
}

/**
 * PipelineApi - interface
 * @export
 * @interface PipelineApi
 */
export interface PipelineApi {
  /**
   * Search pipelines
   * @summary Search pipelines
   * @param {SearchPipelineBody} searchPipelineBody 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {SortObject} [sort] Sort object
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.searchPipelines(null, 1, 1, null);
   * ```
   * @openapi operation=searchPipelines path=/pipelineSearch method=POST
   * @memberof PipelineApi
   */
  searchPipelines(searchPipelineBody: SearchPipelineBody, pageNumber?: number, pageSize?: number, sort?: SortObject): Promise<PagedResults<PipelineExtended>>;

  /**
   * Returns options/filters for pipeline
   * @summary Returns options/filters for pipeline
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @openapi operation=searchPipelinesOptions path=/pipelineSearch/options method=GET
   * @memberof PipelineApi
   */
  searchPipelinesOptions(): Promise<ResultsColumnOptionsWrapper>;

}

/**
 * PipelineProducerApi - interface
 * Producer interface for Pipeline
 * @export
 * @interface PipelineProducerApi
 */
export interface PipelineProducerApi {
  /**
  * Search pipelines
  * @summary Search pipelines
  * @param {SearchPipelineBody} searchPipelineBody 
  * @param {SortObject} [sort] Sort object
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchPipelines path=/pipelineSearch method=POST
  * @memberof Pipeline
  */
  searchPipelines(results: PagedResults<PipelineExtended>, searchPipelineBody: SearchPipelineBody, sort?: SortObject): Promise<void>;
  /**
  * Returns options/filters for pipeline
  * @summary Returns options/filters for pipeline
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchPipelinesOptions path=/pipelineSearch/options method=GET
  * @memberof Pipeline
  */
  searchPipelinesOptions(): Promise<ResultsColumnOptionsWrapper>;
}

export class PipelineApiMappingImpl implements PipelineApi {
  private producer: PipelineProducerApi;
  constructor(producer: PipelineProducerApi) {
    this.producer = producer;
  }

  /**
   * Search pipelines
   * @summary Search pipelines
   * @param searchPipelineBody 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sort Sort object
   * @openapi operation=searchPipelines path=/pipelineSearch method=POST
   */
  public async searchPipelines(searchPipelineBody: SearchPipelineBody, pageNumber: number = 1, pageSize: number = 50, sort?: SortObject): Promise<PagedResults<PipelineExtended>> {
    // verify required parameter 'searchPipelineBody' is not null or undefined
    if (searchPipelineBody === null || searchPipelineBody === undefined) {
      throw new ParameterRequiredError('searchPipelineBody');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<PipelineExtended>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.searchPipelines(bag, searchPipelineBody, sort);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * Returns options/filters for pipeline
   * @summary Returns options/filters for pipeline
   * @openapi operation=searchPipelinesOptions path=/pipelineSearch/options method=GET
   */
  public async searchPipelinesOptions(): Promise<ResultsColumnOptionsWrapper> {
    return this.producer.searchPipelinesOptions();
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapPipelineProducer(producer: PipelineProducerApi): PipelineApi {
  return new PipelineApiMappingImpl(producer);
}

/**
 * PipelineApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class PipelineApiHttpImpl implements PipelineProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async searchPipelines(results: PagedResults<PipelineExtended>, searchPipelineBody: SearchPipelineBody, sort?: SortObject): Promise<void> {
  const _body = {
      searchPipelineBody,sort,
  };
  let _path = '/pipelineSearch';
  return this.client.post(
    _path,
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<PipelineExtended>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/pipelineSearch`);
      results.httpMethod = HttpMethod.Post;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'PipelineExtended');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async searchPipelinesOptions(): Promise<ResultsColumnOptionsWrapper> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/pipelineSearch/options';
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'ResultsColumnOptionsWrapper')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

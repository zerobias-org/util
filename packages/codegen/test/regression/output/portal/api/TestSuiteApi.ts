/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { ResultsColumnOptionsWrapper, SearchTestCaseBody, SearchTestSuiteBody, SortObject, TestCaseExtended, TestSuiteExtended, VspFilters } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace TestSuiteApi {
}

/**
 * TestSuiteApi - interface
 * @export
 * @interface TestSuiteApi
 */
export interface TestSuiteApi {
  /**
   * Get test case
   * @summary Get test case
   * @param {UUID} testSuiteId 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.get('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=getTestSuite path=/testSuites/{testSuiteId} method=GET
   * @memberof TestSuiteApi
   */
  get(testSuiteId: UUID): Promise<TestSuiteExtended>;

  /**
   * Get test suite filters
   * @summary Get test suite filters
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @openapi operation=getTestSuiteFilters path=/testSuiteFilters method=GET
   * @memberof TestSuiteApi
   */
  getFilters(): Promise<VspFilters>;

  /**
   * list all catalog test suites
   * @summary list all catalog test suites
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {string} [search] 
   * @param {SortObject} [sort] Sort object
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list(1, 1, 'search', null);
   * ```
   * @openapi operation=listTestSuites path=/testSuites method=GET
   * @memberof TestSuiteApi
   */
  list(pageNumber?: number, pageSize?: number, search?: string, sort?: SortObject): Promise<PagedResults<TestSuiteExtended>>;

  /**
   * Returns options/filters for test suites
   * @summary Returns options/filters for test suites
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @openapi operation=searchTestSuiteOptions path=/testSuiteSearch/options method=GET
   * @memberof TestSuiteApi
   */
  searchOptions(): Promise<ResultsColumnOptionsWrapper>;

  /**
   * list test cases within a test suite
   * @summary list test cases within a test suite
   * @param {UUID} testSuiteId 
   * @param {SearchTestCaseBody} searchTestCaseBody 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {SortObject} [sort] Sort object
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.searchTestCases('00000000-0000-0000-0000-000000000000', null, 1, 1, null);
   * ```
   * @openapi operation=searchTestSuiteTestCases path=/testSuites/{testSuiteId}/testCaseSearch method=POST
   * @memberof TestSuiteApi
   */
  searchTestCases(testSuiteId: UUID, searchTestCaseBody: SearchTestCaseBody, pageNumber?: number, pageSize?: number, sort?: SortObject): Promise<PagedResults<TestCaseExtended>>;

  /**
   * list all catalog test suites
   * @summary list all catalog test suites
   * @param {SearchTestSuiteBody} searchTestSuiteBody 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {SortObject} [sort] Sort object
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.search(null, 1, 1, null);
   * ```
   * @openapi operation=searchTestSuites path=/testSuiteSearch method=POST
   * @memberof TestSuiteApi
   */
  search(searchTestSuiteBody: SearchTestSuiteBody, pageNumber?: number, pageSize?: number, sort?: SortObject): Promise<PagedResults<TestSuiteExtended>>;

}

/**
 * TestSuiteProducerApi - interface
 * Producer interface for TestSuite
 * @export
 * @interface TestSuiteProducerApi
 */
export interface TestSuiteProducerApi {
  /**
  * Get test case
  * @summary Get test case
  * @param {UUID} testSuiteId 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getTestSuite path=/testSuites/{testSuiteId} method=GET
  * @memberof TestSuite
  */
  get(testSuiteId: UUID): Promise<TestSuiteExtended>;
  /**
  * Get test suite filters
  * @summary Get test suite filters
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getTestSuiteFilters path=/testSuiteFilters method=GET
  * @memberof TestSuite
  */
  getFilters(): Promise<VspFilters>;
  /**
  * list all catalog test suites
  * @summary list all catalog test suites
  * @param {string} [search] 
  * @param {SortObject} [sort] Sort object
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listTestSuites path=/testSuites method=GET
  * @memberof TestSuite
  */
  list(results: PagedResults<TestSuiteExtended>, search?: string, sort?: SortObject): Promise<void>;
  /**
  * Returns options/filters for test suites
  * @summary Returns options/filters for test suites
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchTestSuiteOptions path=/testSuiteSearch/options method=GET
  * @memberof TestSuite
  */
  searchOptions(): Promise<ResultsColumnOptionsWrapper>;
  /**
  * list test cases within a test suite
  * @summary list test cases within a test suite
  * @param {UUID} testSuiteId 
  * @param {SearchTestCaseBody} searchTestCaseBody 
  * @param {SortObject} [sort] Sort object
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchTestSuiteTestCases path=/testSuites/{testSuiteId}/testCaseSearch method=POST
  * @memberof TestSuite
  */
  searchTestCases(results: PagedResults<TestCaseExtended>, testSuiteId: UUID, searchTestCaseBody: SearchTestCaseBody, sort?: SortObject): Promise<void>;
  /**
  * list all catalog test suites
  * @summary list all catalog test suites
  * @param {SearchTestSuiteBody} searchTestSuiteBody 
  * @param {SortObject} [sort] Sort object
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchTestSuites path=/testSuiteSearch method=POST
  * @memberof TestSuite
  */
  search(results: PagedResults<TestSuiteExtended>, searchTestSuiteBody: SearchTestSuiteBody, sort?: SortObject): Promise<void>;
}

export class TestSuiteApiMappingImpl implements TestSuiteApi {
  private producer: TestSuiteProducerApi;
  constructor(producer: TestSuiteProducerApi) {
    this.producer = producer;
  }

  /**
   * Get test case
   * @summary Get test case
   * @param testSuiteId 
   * @openapi operation=getTestSuite path=/testSuites/{testSuiteId} method=GET
   */
  public async get(testSuiteId: UUID): Promise<TestSuiteExtended> {
    // verify required parameter 'testSuiteId' is not null or undefined
    if (testSuiteId === null || testSuiteId === undefined) {
      throw new ParameterRequiredError('testSuiteId');
    }
    return this.producer.get(testSuiteId);
  }
  /**
   * Get test suite filters
   * @summary Get test suite filters
   * @openapi operation=getTestSuiteFilters path=/testSuiteFilters method=GET
   */
  public async getFilters(): Promise<VspFilters> {
    return this.producer.getFilters();
  }
  /**
   * list all catalog test suites
   * @summary list all catalog test suites
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param search 
   * @param sort Sort object
   * @openapi operation=listTestSuites path=/testSuites method=GET
   */
  public async list(pageNumber: number = 1, pageSize: number = 50, search?: string, sort?: SortObject): Promise<PagedResults<TestSuiteExtended>> {
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<TestSuiteExtended>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.list(bag, search, sort);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * Returns options/filters for test suites
   * @summary Returns options/filters for test suites
   * @openapi operation=searchTestSuiteOptions path=/testSuiteSearch/options method=GET
   */
  public async searchOptions(): Promise<ResultsColumnOptionsWrapper> {
    return this.producer.searchOptions();
  }
  /**
   * list test cases within a test suite
   * @summary list test cases within a test suite
   * @param testSuiteId 
   * @param searchTestCaseBody 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sort Sort object
   * @openapi operation=searchTestSuiteTestCases path=/testSuites/{testSuiteId}/testCaseSearch method=POST
   */
  public async searchTestCases(testSuiteId: UUID, searchTestCaseBody: SearchTestCaseBody, pageNumber: number = 1, pageSize: number = 50, sort?: SortObject): Promise<PagedResults<TestCaseExtended>> {
    // verify required parameter 'testSuiteId' is not null or undefined
    if (testSuiteId === null || testSuiteId === undefined) {
      throw new ParameterRequiredError('testSuiteId');
    }
    // verify required parameter 'searchTestCaseBody' is not null or undefined
    if (searchTestCaseBody === null || searchTestCaseBody === undefined) {
      throw new ParameterRequiredError('searchTestCaseBody');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<TestCaseExtended>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.searchTestCases(bag, testSuiteId, searchTestCaseBody, sort);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * list all catalog test suites
   * @summary list all catalog test suites
   * @param searchTestSuiteBody 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sort Sort object
   * @openapi operation=searchTestSuites path=/testSuiteSearch method=POST
   */
  public async search(searchTestSuiteBody: SearchTestSuiteBody, pageNumber: number = 1, pageSize: number = 50, sort?: SortObject): Promise<PagedResults<TestSuiteExtended>> {
    // verify required parameter 'searchTestSuiteBody' is not null or undefined
    if (searchTestSuiteBody === null || searchTestSuiteBody === undefined) {
      throw new ParameterRequiredError('searchTestSuiteBody');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<TestSuiteExtended>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.search(bag, searchTestSuiteBody, sort);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapTestSuiteProducer(producer: TestSuiteProducerApi): TestSuiteApi {
  return new TestSuiteApiMappingImpl(producer);
}

/**
 * TestSuiteApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class TestSuiteApiHttpImpl implements TestSuiteProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async get(testSuiteId: UUID): Promise<TestSuiteExtended> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/testSuites/{testSuiteId}';
  _path = _path.replace('{testSuiteId}', encodeURIComponent(String(testSuiteId)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'TestSuiteExtended')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async getFilters(): Promise<VspFilters> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/testSuiteFilters';
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'VspFilters')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(results: PagedResults<TestSuiteExtended>, search?: string, sort?: SortObject): Promise<void> {
  const _body = {
      search,sort,
  };
  let _path = '/testSuites';
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<TestSuiteExtended>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/testSuites`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'TestSuiteExtended');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async searchOptions(): Promise<ResultsColumnOptionsWrapper> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/testSuiteSearch/options';
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'ResultsColumnOptionsWrapper')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async searchTestCases(results: PagedResults<TestCaseExtended>, testSuiteId: UUID, searchTestCaseBody: SearchTestCaseBody, sort?: SortObject): Promise<void> {
  const _body = {
      testSuiteId,searchTestCaseBody,sort,
  };
  let _path = '/testSuites/{testSuiteId}/testCaseSearch';
  _path = _path.replace('{testSuiteId}', encodeURIComponent(String(testSuiteId)));
  return this.client.post(
    _path,
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<TestCaseExtended>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/testSuites/{testSuiteId}/testCaseSearch`);
      results.httpMethod = HttpMethod.Post;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'TestCaseExtended');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async search(results: PagedResults<TestSuiteExtended>, searchTestSuiteBody: SearchTestSuiteBody, sort?: SortObject): Promise<void> {
  const _body = {
      searchTestSuiteBody,sort,
  };
  let _path = '/testSuiteSearch';
  return this.client.post(
    _path,
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<TestSuiteExtended>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/testSuiteSearch`);
      results.httpMethod = HttpMethod.Post;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'TestSuiteExtended');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { Permission } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Nmtoken, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace PermissionApi {
}

/**
 * PermissionApi - interface
 * @export
 * @interface PermissionApi
 */
export interface PermissionApi {
  /**
   * 
   * @summary Retrieves a list of permissions that are effective against the currently logged on user  and the given resourceId or resourceType.  Note that, if a resourceId is provied, resourceType will not be evaluated.
   * @param {UUID} [resourceId] ID of the resource to check current user permissions against
   * @param {Nmtoken} [resourceType] Type of resource to check current user permissions against
   * @param {UUID} [boundaryId] An optional resource boundaryId to filter with.  This has no implementation yet.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.listEffective('00000000-0000-0000-0000-000000000000', null, '00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=listEffectivePermissions path=/effectivePermissions method=GET
   * @memberof PermissionApi
   */
  listEffective(resourceId?: UUID, resourceType?: Nmtoken, boundaryId?: UUID): Promise<Array<Permission>>;

  /**
   * 
   * @summary Retrieves a list of all available permissions
   * @param {Nmtoken} [resourceType] Type of resource to filter permissions with
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list(null);
   * ```
   * @openapi operation=listPermisions path=/permissions method=GET
   * @memberof PermissionApi
   */
  list(resourceType?: Nmtoken): Promise<Array<Permission>>;

}

/**
 * PermissionProducerApi - interface
 * Producer interface for Permission
 * @export
 * @interface PermissionProducerApi
 */
export interface PermissionProducerApi {
  /**
  * 
  * @summary Retrieves a list of permissions that are effective against the currently logged on user  and the given resourceId or resourceType.  Note that, if a resourceId is provied, resourceType will not be evaluated.
  * @param {UUID} [resourceId] ID of the resource to check current user permissions against
  * @param {Nmtoken} [resourceType] Type of resource to check current user permissions against
  * @param {UUID} [boundaryId] An optional resource boundaryId to filter with.  This has no implementation yet.
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listEffectivePermissions path=/effectivePermissions method=GET
  * @memberof Permission
  */
  listEffective(resourceId?: UUID, resourceType?: Nmtoken, boundaryId?: UUID): Promise<Array<Permission>>;
  /**
  * 
  * @summary Retrieves a list of all available permissions
  * @param {Nmtoken} [resourceType] Type of resource to filter permissions with
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=listPermisions path=/permissions method=GET
  * @memberof Permission
  */
  list(resourceType?: Nmtoken): Promise<Array<Permission>>;
}

export class PermissionApiMappingImpl implements PermissionApi {
  private producer: PermissionProducerApi;
  constructor(producer: PermissionProducerApi) {
    this.producer = producer;
  }

  /**
   * 
   * @summary Retrieves a list of permissions that are effective against the currently logged on user  and the given resourceId or resourceType.  Note that, if a resourceId is provied, resourceType will not be evaluated.
   * @param resourceId ID of the resource to check current user permissions against
   * @param resourceType Type of resource to check current user permissions against
   * @param boundaryId An optional resource boundaryId to filter with.  This has no implementation yet.
   * @openapi operation=listEffectivePermissions path=/effectivePermissions method=GET
   */
  public async listEffective(resourceId?: UUID, resourceType?: Nmtoken, boundaryId?: UUID): Promise<Array<Permission>> {
    return this.producer.listEffective(resourceId, resourceType, boundaryId);
  }
  /**
   * 
   * @summary Retrieves a list of all available permissions
   * @param resourceType Type of resource to filter permissions with
   * @openapi operation=listPermisions path=/permissions method=GET
   */
  public async list(resourceType?: Nmtoken): Promise<Array<Permission>> {
    return this.producer.list(resourceType);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapPermissionProducer(producer: PermissionProducerApi): PermissionApi {
  return new PermissionApiMappingImpl(producer);
}

/**
 * PermissionApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class PermissionApiHttpImpl implements PermissionProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async listEffective(resourceId?: UUID, resourceType?: Nmtoken, boundaryId?: UUID): Promise<Array<Permission>> {
  const _queryParams = {
      resourceId,resourceType,boundaryId,
  };
  const _bodyParams = {
      
  };
  let _path = '/effectivePermissions';
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Array<Permission>')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(resourceType?: Nmtoken): Promise<Array<Permission>> {
  const _queryParams = {
      resourceType,
  };
  const _bodyParams = {
      
  };
  let _path = '/permissions';
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Array<Permission>')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

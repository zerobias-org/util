/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { AxiosInstance } from 'axios';
import { ConnectionMetadata, ExecutionInfo, OperationSupportStatusDef, ResultsColumnOptionsWrapper, SearchTargetBody, SortDirectionDef, SortObject, Tag, Target, TargetSearchView } from '../model/index.js';
import { URL, EnumValue, IllegalArgumentError, PagedResults, Int32, UUID, CoreError, ParameterRequiredError, UnexpectedError, HttpMethod } from '@zerobias-org/types-core-js';
import { RequestFile, ObjectSerializer } from '../model/index.js';

export namespace TargetApi {
}

/**
 * TargetApi - interface
 * @export
 * @interface TargetApi
 */
export interface TargetApi {
  /**
   * Executes the given operation on the given target.
   * @summary Executes a request against a target
   * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @param {string} operationId The name of the operation to execute
   * @param {{ [key: string]: any; }} requestBody 
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.execute('00000000-0000-0000-0000-000000000000', 'operationId', null);
   * ```
   * @openapi operation=execute path=/targets/{id}/{operationId} method=PUT
   * @memberof TargetApi
   */
  execute(id: UUID, operationId: string, requestBody: { [key: string]: any; }): Promise<any>;

  /**
   * 
   * @summary Get a Target
   * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.get('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=get path=/targets/{id} method=GET
   * @memberof TargetApi
   */
  get(id: UUID): Promise<Target>;

  /**
   * 
   * @summary Lists all execution info for a target
   * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {Array<SortDirection>} [sortDir] Array of directions to sort by. Must correspond with &#x60;sortBy&#x60;
   * @param {Array<string>} [sortBy] Array of columns to sort by, in order
   * @param {UUID} [nodeId] Filters executions by node id
   * @param {string} [operationName] Filters executions by operation name
   * @param {boolean} [status] Filters executions by status of the executions response
   * @param {Date} [startTimestamp] Filters executions by timestamp, will show only executions after timestamp given
   * @param {Date} [endTimestamp] Filters executions by timestamp, will show only executions before timestamp given
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.getTargetExecutions('00000000-0000-0000-0000-000000000000', 1, 1, null, null, '00000000-0000-0000-0000-000000000000', 'operationName', true, '2020-01-01T00:00:00Z', '2020-01-01T00:00:00Z');
   * ```
   * @openapi operation=getTargetExecutions path=/targets{id}/logs method=GET
   * @memberof TargetApi
   */
  getTargetExecutions(id: UUID, pageNumber?: number, pageSize?: number, sortDir?: Array<SortDirectionDef>, sortBy?: Array<string>, nodeId?: UUID, operationName?: string, status?: boolean, startTimestamp?: Date, endTimestamp?: Date): Promise<PagedResults<ExecutionInfo>>;

  /**
   * 
   * @summary Returns the metadata from the Target. If this Target is not a `Connection`, then nothing is returned
   * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation  of the ModuleVersion. If a ConnectionProfile is required, then targets must be  Connections. Else, they can be Deployments.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.getTargetMetadata('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=getTargetMetadata path=/targets/{id}/metadata method=GET
   * @memberof TargetApi
   */
  getTargetMetadata(id: UUID): Promise<ConnectionMetadata>;

  /**
   * 
   * @summary Returns all Tags associated with the Target. This cascades up to include tags on the  Connection, Deployment, and Node.
   * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.getTargetTags('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=getTargetTags path=/targets/{id}/tags method=GET
   * @memberof TargetApi
   */
  getTargetTags(id: UUID): Promise<Array<Tag>>;

  /**
   * 
   * @summary Checks if a given operation is supported
   * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @param {string} operationId The name of the operation to execute
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.isSupported('00000000-0000-0000-0000-000000000000', 'operationId');
   * ```
   * @openapi operation=isSupported path=/targets/{id}/{operationId}/supported method=GET
   * @memberof TargetApi
   */
  isSupported(id: UUID, operationId: string): Promise<OperationSupportStatusDef>;

  /**
   * 
   * @summary Lists all targets
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {string} [filter] An optional filter to search tags by name. This is a case-insensitive filter
   * @param {UUID} [nodeId] Filters targets to only those matching the given nodeId
   * @param {UUID} [moduleId] Filters targets to only those matching the given moduleId
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.list(1, 1, 'filter', '00000000-0000-0000-0000-000000000000', '00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=list path=/targets method=GET
   * @memberof TargetApi
   */
  list(pageNumber?: number, pageSize?: number, filter?: string, nodeId?: UUID, moduleId?: UUID): Promise<PagedResults<Target>>;

  /**
   * Search targets
   * @param {SearchTargetBody} searchTargetBody 
   * @param {number} [pageNumber] The requested page. This value is 1-indexed.
   * @param {number} [pageSize] The number of items in each page of data.
   * @param {SortObject} [sort] 
   * @param {UUID} [boundaryId] The boundaryId to scope this request as
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.search(null, 1, 1, null, '00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=searchTargets path=/targetSearch method=POST
   * @memberof TargetApi
   */
  search(searchTargetBody: SearchTargetBody, pageNumber?: number, pageSize?: number, sort?: SortObject, boundaryId?: UUID): Promise<PagedResults<TargetSearchView>>;

  /**
   * Returns options/filters for targets
   * @param {UUID} [boundaryId] The boundaryId to scope this request as
   * @param {*} [options] Override http request option.
   * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
   * @example
   * ```typescript
   * const result = await api.searchOptions('00000000-0000-0000-0000-000000000000');
   * ```
   * @openapi operation=searchTargetsOptions path=/targetSearch/options method=GET
   * @memberof TargetApi
   */
  searchOptions(boundaryId?: UUID): Promise<ResultsColumnOptionsWrapper>;

}

/**
 * TargetProducerApi - interface
 * Producer interface for Target
 * @export
 * @interface TargetProducerApi
 */
export interface TargetProducerApi {
  /**
  * Executes the given operation on the given target.
  * @summary Executes a request against a target
  * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
  * @param {string} operationId The name of the operation to execute
  * @param {{ [key: string]: any; }} requestBody 
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=execute path=/targets/{id}/{operationId} method=PUT
  * @memberof Target
  */
  execute(id: UUID, operationId: string, requestBody: { [key: string]: any; }): Promise<any>;
  /**
  * 
  * @summary Get a Target
  * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=get path=/targets/{id} method=GET
  * @memberof Target
  */
  get(id: UUID): Promise<Target>;
  /**
  * 
  * @summary Lists all execution info for a target
  * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
  * @param {UUID} [nodeId] Filters executions by node id
  * @param {string} [operationName] Filters executions by operation name
  * @param {boolean} [status] Filters executions by status of the executions response
  * @param {Date} [startTimestamp] Filters executions by timestamp, will show only executions after timestamp given
  * @param {Date} [endTimestamp] Filters executions by timestamp, will show only executions before timestamp given
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getTargetExecutions path=/targets{id}/logs method=GET
  * @memberof Target
  */
  getTargetExecutions(results: PagedResults<ExecutionInfo>, id: UUID, nodeId?: UUID, operationName?: string, status?: boolean, startTimestamp?: Date, endTimestamp?: Date): Promise<void>;
  /**
  * 
  * @summary Returns the metadata from the Target. If this Target is not a `Connection`, then nothing is returned
  * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation  of the ModuleVersion. If a ConnectionProfile is required, then targets must be  Connections. Else, they can be Deployments.
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getTargetMetadata path=/targets/{id}/metadata method=GET
  * @memberof Target
  */
  getTargetMetadata(id: UUID): Promise<ConnectionMetadata>;
  /**
  * 
  * @summary Returns all Tags associated with the Target. This cascades up to include tags on the  Connection, Deployment, and Node.
  * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=getTargetTags path=/targets/{id}/tags method=GET
  * @memberof Target
  */
  getTargetTags(id: UUID): Promise<Array<Tag>>;
  /**
  * 
  * @summary Checks if a given operation is supported
  * @param {UUID} id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
  * @param {string} operationId The name of the operation to execute
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=isSupported path=/targets/{id}/{operationId}/supported method=GET
  * @memberof Target
  */
  isSupported(id: UUID, operationId: string): Promise<OperationSupportStatusDef>;
  /**
  * 
  * @summary Lists all targets
  * @param {string} [filter] An optional filter to search tags by name. This is a case-insensitive filter
  * @param {UUID} [nodeId] Filters targets to only those matching the given nodeId
  * @param {UUID} [moduleId] Filters targets to only those matching the given moduleId
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=list path=/targets method=GET
  * @memberof Target
  */
  list(results: PagedResults<Target>, filter?: string, nodeId?: UUID, moduleId?: UUID): Promise<void>;
  /**
  * Search targets
  * @param {SearchTargetBody} searchTargetBody 
  * @param {SortObject} [sort] 
  * @param {UUID} [boundaryId] The boundaryId to scope this request as
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchTargets path=/targetSearch method=POST
  * @memberof Target
  */
  search(results: PagedResults<TargetSearchView>, searchTargetBody: SearchTargetBody, sort?: SortObject, boundaryId?: UUID): Promise<void>;
  /**
  * Returns options/filters for targets
  * @param {UUID} [boundaryId] The boundaryId to scope this request as
  * @param {*} [options] Override http request option.
  * @throws {@link @zerobias-org/types-core-js#ParameterRequiredError} if any required parameter is not provided
  * @openapi operation=searchTargetsOptions path=/targetSearch/options method=GET
  * @memberof Target
  */
  searchOptions(boundaryId?: UUID): Promise<ResultsColumnOptionsWrapper>;
}

export class TargetApiMappingImpl implements TargetApi {
  private producer: TargetProducerApi;
  constructor(producer: TargetProducerApi) {
    this.producer = producer;
  }

  /**
   * Executes the given operation on the given target.
   * @summary Executes a request against a target
   * @param id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @param operationId The name of the operation to execute
   * @param requestBody 
   * @openapi operation=execute path=/targets/{id}/{operationId} method=PUT
   */
  public async execute(id: UUID, operationId: string, requestBody: { [key: string]: any; }): Promise<any> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    // verify required parameter 'operationId' is not null or undefined
    if (operationId === null || operationId === undefined || operationId === '') {
      throw new ParameterRequiredError('operationId');
    }
    // verify required parameter 'requestBody' is not null or undefined
    if (requestBody === null || requestBody === undefined) {
      throw new ParameterRequiredError('requestBody');
    }
    return this.producer.execute(id, operationId, requestBody);
  }
  /**
   * 
   * @summary Get a Target
   * @param id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @openapi operation=get path=/targets/{id} method=GET
   */
  public async get(id: UUID): Promise<Target> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.get(id);
  }
  /**
   * 
   * @summary Lists all execution info for a target
   * @param id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sortDir Array of directions to sort by. Must correspond with &#x60;sortBy&#x60;
   * @param sortBy Array of columns to sort by, in order
   * @param nodeId Filters executions by node id
   * @param operationName Filters executions by operation name
   * @param status Filters executions by status of the executions response
   * @param startTimestamp Filters executions by timestamp, will show only executions after timestamp given
   * @param endTimestamp Filters executions by timestamp, will show only executions before timestamp given
   * @openapi operation=getTargetExecutions path=/targets{id}/logs method=GET
   */
  public async getTargetExecutions(id: UUID, pageNumber: number = 1, pageSize: number = 50, sortDir?: Array<SortDirectionDef>, sortBy?: Array<string>, nodeId?: UUID, operationName?: string, status?: boolean, startTimestamp?: Date, endTimestamp?: Date): Promise<PagedResults<ExecutionInfo>> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<ExecutionInfo>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    if (sortBy && sortDir) {
      bag.sort(sortBy, sortDir);
    }
    await this.producer.getTargetExecutions(bag, id, nodeId, operationName, status, startTimestamp, endTimestamp);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * 
   * @summary Returns the metadata from the Target. If this Target is not a `Connection`, then nothing is returned
   * @param id A Target ID is either a Deployment or Connection ID, depending on the implementation  of the ModuleVersion. If a ConnectionProfile is required, then targets must be  Connections. Else, they can be Deployments.
   * @openapi operation=getTargetMetadata path=/targets/{id}/metadata method=GET
   */
  public async getTargetMetadata(id: UUID): Promise<ConnectionMetadata> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.getTargetMetadata(id);
  }
  /**
   * 
   * @summary Returns all Tags associated with the Target. This cascades up to include tags on the  Connection, Deployment, and Node.
   * @param id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @openapi operation=getTargetTags path=/targets/{id}/tags method=GET
   */
  public async getTargetTags(id: UUID): Promise<Array<Tag>> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    return this.producer.getTargetTags(id);
  }
  /**
   * 
   * @summary Checks if a given operation is supported
   * @param id A Target ID is either a Deployment or Connection ID, depending on the implementation of the ModuleVersion. If a ConnectionProfile is required, then targets must be Connections. Else, they can be Deployments.
   * @param operationId The name of the operation to execute
   * @openapi operation=isSupported path=/targets/{id}/{operationId}/supported method=GET
   */
  public async isSupported(id: UUID, operationId: string): Promise<OperationSupportStatusDef> {
    // verify required parameter 'id' is not null or undefined
    if (id === null || id === undefined) {
      throw new ParameterRequiredError('id');
    }
    // verify required parameter 'operationId' is not null or undefined
    if (operationId === null || operationId === undefined || operationId === '') {
      throw new ParameterRequiredError('operationId');
    }
    return this.producer.isSupported(id, operationId);
  }
  /**
   * 
   * @summary Lists all targets
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param filter An optional filter to search tags by name. This is a case-insensitive filter
   * @param nodeId Filters targets to only those matching the given nodeId
   * @param moduleId Filters targets to only those matching the given moduleId
   * @openapi operation=list path=/targets method=GET
   */
  public async list(pageNumber: number = 1, pageSize: number = 50, filter?: string, nodeId?: UUID, moduleId?: UUID): Promise<PagedResults<Target>> {
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<Target>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.list(bag, filter, nodeId, moduleId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * Search targets
   * @param searchTargetBody 
   * @param pageNumber The requested page. This value is 1-indexed.
   * @param pageSize The number of items in each page of data.
   * @param sort 
   * @param boundaryId The boundaryId to scope this request as
   * @openapi operation=searchTargets path=/targetSearch method=POST
   */
  public async search(searchTargetBody: SearchTargetBody, pageNumber: number = 1, pageSize: number = 50, sort?: SortObject, boundaryId?: UUID): Promise<PagedResults<TargetSearchView>> {
    // verify required parameter 'searchTargetBody' is not null or undefined
    if (searchTargetBody === null || searchTargetBody === undefined) {
      throw new ParameterRequiredError('searchTargetBody');
    }
    if (pageNumber) {
      await Int32.parse(pageNumber);
    }
    if (pageSize) {
      await Int32.parse(pageSize);
    }
    const bag = new PagedResults<TargetSearchView>();
    bag.pageNumber = pageNumber;
    bag.pageSize = pageSize;
    await this.producer.search(bag, searchTargetBody, sort, boundaryId);
    if (bag.items === null || bag.items === undefined) {
      throw new UnexpectedError("Producers must return 'items' for PagedResults queries");
    }
    return bag;
  }
  /**
   * Returns options/filters for targets
   * @param boundaryId The boundaryId to scope this request as
   * @openapi operation=searchTargetsOptions path=/targetSearch/options method=GET
   */
  public async searchOptions(boundaryId?: UUID): Promise<ResultsColumnOptionsWrapper> {
    return this.producer.searchOptions(boundaryId);
  }
}

/**
 * Wrap producer with mapping implementation
 */
export function wrapTargetProducer(producer: TargetProducerApi): TargetApi {
  return new TargetApiMappingImpl(producer);
}

/**
 * TargetApiHttpImpl - Direct HTTP implementation
 *
 * Makes direct HTTP calls to the API using Axios.
 * Used by SDK clients for platform service access.
 */
export class TargetApiHttpImpl implements TargetProducerApi {
  private client: AxiosInstance;
  private headers: Record<string, string>;

  constructor(client: AxiosInstance, headers: Record<string, string> = {}) {
    this.client = client;
    this.headers = headers;
  }

  async execute(id: UUID, operationId: string, requestBody: { [key: string]: any; }): Promise<any> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      requestBody,
  };
  let _path = '/targets/{id}/{operationId}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  _path = _path.replace('{operationId}', encodeURIComponent(String(operationId)));
  return this.client.put(
    _path,
    _bodyParams)
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'any')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async get(id: UUID): Promise<Target> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/targets/{id}';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Target')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async getTargetExecutions(results: PagedResults<ExecutionInfo>, id: UUID, nodeId?: UUID, operationName?: string, status?: boolean, startTimestamp?: Date, endTimestamp?: Date): Promise<void> {
  const _body = {
      id,nodeId,operationName,status,startTimestamp,endTimestamp,
  };
  let _path = '/targets{id}/logs';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<ExecutionInfo>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/targets{id}/logs`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'ExecutionInfo');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async getTargetMetadata(id: UUID): Promise<ConnectionMetadata> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/targets/{id}/metadata';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'ConnectionMetadata')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async getTargetTags(id: UUID): Promise<Array<Tag>> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/targets/{id}/tags';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'Array<Tag>')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async isSupported(id: UUID, operationId: string): Promise<OperationSupportStatusDef> {
  const _queryParams = {
      
  };
  const _bodyParams = {
      
  };
  let _path = '/targets/{id}/{operationId}/supported';
  _path = _path.replace('{id}', encodeURIComponent(String(id)));
  _path = _path.replace('{operationId}', encodeURIComponent(String(operationId)));
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'OperationSupportStatusDef')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async list(results: PagedResults<Target>, filter?: string, nodeId?: UUID, moduleId?: UUID): Promise<void> {
  const _body = {
      filter,nodeId,moduleId,
  };
  let _path = '/targets';
  return this.client.get(
    _path,
    {
      params: {
        ..._body,
        pageNumber: results.pageNumber,
        pageSize: results.pageSize,
      }
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<Target>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/targets`);
      results.httpMethod = HttpMethod.Get;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'Target');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async search(results: PagedResults<TargetSearchView>, searchTargetBody: SearchTargetBody, sort?: SortObject, boundaryId?: UUID): Promise<void> {
  const _body = {
      searchTargetBody,sort,boundaryId,
  };
  let _path = '/targetSearch';
  return this.client.post(
    _path,
    {
      ..._body,
      pageNumber: results.pageNumber,
      pageSize: results.pageSize,
    })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      results.items = ObjectSerializer.deserialize(response.data, 'Array<TargetSearchView>');
      results.count = response.headers.count ? Number(response.headers.count) : -1;
      results.pageToken = response.headers.pagetoken ?? response.headers.pageToken;
      results.headers = this.headers;
      results.baseUrl = new URL(`${this.client.getUri()}/targetSearch`);
      results.httpMethod = HttpMethod.Post;
      results.body = _body;
      results.mapper = (obj) => ObjectSerializer.deserialize(obj, 'TargetSearchView');
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
  async searchOptions(boundaryId?: UUID): Promise<ResultsColumnOptionsWrapper> {
  const _queryParams = {
      boundaryId,
  };
  const _bodyParams = {
      
  };
  let _path = '/targetSearch/options';
  return this.client.get(
    _path,
    { params: _queryParams })
    .then((response) => {
      // BaseApiClient uses validateStatus: () => true, so we must check status manually
      if (response.status >= 400) {
        throw CoreError.deserialize(response.data);
      }
      return ObjectSerializer.deserialize(response.data, 'ResultsColumnOptionsWrapper')
    })
    .catch((error) => {
      if (error.data) {
        throw CoreError.deserialize(error.data);
      } else {
        throw error;
      }
    });
  }
}

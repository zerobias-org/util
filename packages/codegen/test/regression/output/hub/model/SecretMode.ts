/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { EnumValue, IllegalArgumentError } from '@zerobias-org/types-core-js';
import { ObjectSerializer, RequestFile } from './index.js';

/**
* Mode of operation for a stored secret
*/
export const SecretMode = {
  /** Secret values themselves are managed via Hub APIs */
  Managed: EnumValue.instance('SecretMode', 'Managed', 'managed'),
  /** Secret paths are selected from an existing secrets manager */
  Selected: EnumValue.instance('SecretMode', 'Selected', 'selected'),
  /** Secret is managed via OAuth */
  Oauth: EnumValue.instance('SecretMode', 'Oauth', 'oauth'),

  from(val: string | number): EnumValue {
    if (SecretMode[val]) {
      return SecretMode[val];
    }
    const byValue = SecretMode.values.find((v) => v.value === val);
    if (byValue) {
      return byValue;
    }
    throw new IllegalArgumentError(`${val} is not a valid SecretMode`);
  },

  get values(): EnumValue[] {
    return Object
      .keys(SecretMode)
      .filter((k) => k !== 'values' && k !== 'from')
      .map((t) => SecretMode[t]);
  }
} as const;

export type SecretModeDef = typeof SecretMode[keyof typeof SecretMode];

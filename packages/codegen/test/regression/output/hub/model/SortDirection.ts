/* eslint-disable */
// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================
import { EnumValue, IllegalArgumentError } from '@zerobias-org/types-core-js';
import { ObjectSerializer, RequestFile } from './index.js';

export const SortDirection = {
  /** Ascending */
  Asc: EnumValue.instance('SortDirection', 'Asc', 'asc'),
  /** Descending */
  Desc: EnumValue.instance('SortDirection', 'Desc', 'desc'),

  from(val: string | number): EnumValue {
    if (SortDirection[val]) {
      return SortDirection[val];
    }
    const byValue = SortDirection.values.find((v) => v.value === val);
    if (byValue) {
      return byValue;
    }
    throw new IllegalArgumentError(`${val} is not a valid SortDirection`);
  },

  get values(): EnumValue[] {
    return Object
      .keys(SortDirection)
      .filter((k) => k !== 'values' && k !== 'from')
      .map((t) => SortDirection[t]);
  }
} as const;

export type SortDirectionDef = typeof SortDirection[keyof typeof SortDirection];

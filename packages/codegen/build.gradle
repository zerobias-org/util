plugins {
  id 'java-library'
  id 'zb.maven-central-publish'
}

group = 'com.zerobias.hub'
// Auto-bump patch is delegated to the zb.maven-central-publish plugin,
// which resolves project.version by querying Maven Central + GitHub
// Packages metadata and picking the next free patch in 2.0.* . GitHub
// Packages has historical codegen publishes up to 2.0.50 — the resolver
// checks both sources to avoid collisions during the Central cutover.
// Artifact id comes from base.archivesName = 'hub-module-codegen' below.
mavenCentralPublish {
  baseVersion = '2.0'
}
description = 'OpenAPI Generator for creating Hub Modules'

// Force the artifact name regardless of how gradle is invoked (e.g. from
// the monorepo root vs from this directory). Downstream npm consumers
// (bin/hub-generator.js, copyDeps) expect hub-module-codegen-*.jar.
base {
    archivesName = 'hub-module-codegen'
}

java {
  sourceCompatibility = JavaVersion.VERSION_1_8
  targetCompatibility = JavaVersion.VERSION_1_8
}

tasks.withType(JavaCompile) {
  options.fork = true
  options.encoding = 'UTF-8'
  options.compilerArgs << "-Xlint:unchecked" << "-Xlint:deprecation"
  options.compilerArgs << "-Xlint:fallthrough" << "-Xlint:finally"
  options.compilerArgs << "-parameters"
}

tasks.withType(Javadoc) {
  options.encoding = 'UTF-8'
  options.addStringOption('Xdoclint:none', '-quiet')
}

processResources {
  from ('package.json')
}

repositories {
  mavenLocal()
  maven {
    name = 'GitHubPackages ds-commons'
    url = uri('https://maven.pkg.github.com/devsupply/ds-commons')
    credentials {
      username = System.getenv('GITHUB_ACTOR')
      password = System.getenv('GITHUB_TOKEN')
    }
  }
  maven {
    name = 'GitHubPackages'
    url = uri('https://maven.pkg.github.com/zerobias-org/util')
    credentials {
      username = System.getenv('GITHUB_ACTOR')
      password = System.getenv('GITHUB_TOKEN')
    }
  }
  mavenCentral()
}

dependencies {
  implementation 'org.openapitools:openapi-generator-cli:5.4.0'
}

task copyDependencies(type: Copy) {
   from configurations.compileClasspath
   into "${project.layout.buildDirectory.get().asFile}/dependencies"
}
build.dependsOn += copyDependencies

// Stage the built hub-module-codegen jar and the openapi-generator-cli
// runtime jar into `bin/` so `bin/hub-generator.js` can invoke them.
// Locally this is normally done by `npm run copyDeps`; monorepoBuild
// only runs gradle's :build and never invokes that npm script, so wire
// the staging directly into :build instead. This keeps local + CI
// behavior identical for downstream packages (e.g. hub-module-utils)
// that invoke `hub-generator`.
task stageBinJars(type: Copy) {
    dependsOn jar, copyDependencies
    from jar.archiveFile
    from fileTree("${project.layout.buildDirectory.get().asFile}/dependencies") {
        include 'openapi-generator-cli-*.jar'
    }
    into "$projectDir/bin"
    rename(/hub-module-codegen-.*\.jar/, 'hub-module-codegen.jar')
    rename(/openapi-generator-cli-.*\.jar/, 'openapi-generator-cli.jar')
}
build.dependsOn += stageBinJars

test {
    useTestNG()
    // src/test/java has a placeholder class with no @Test methods so the
    // task has source (avoids state.noSource → gate "skipped"). The real
    // unit tests run via mochaUnit (below). Property only exists on
    // Gradle 9+; root invokes via Gradle 8.10, codegen-direct uses 9.3.
    if (it.hasProperty('failOnNoDiscoveredTests')) {
        failOnNoDiscoveredTests = false
    }
    dependsOn 'mochaUnit'
}

task mochaUnit(type: Exec) {
    workingDir projectDir
    commandLine 'node', '--import', 'tsx/esm', '../../node_modules/mocha/bin/mocha.js',
                '--exit', '--extensions', 'ts', 'test/unit/dateNormalization.spec.ts'
}

// Vanniktech reads artifactId from project.name. codegen's
// rootProject.name in settings.gradle is 'hub-module-codegen' — matches
// the desired Maven coordinate + the jar name (base.archivesName).

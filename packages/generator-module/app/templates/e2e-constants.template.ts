/**
 * Test constants — resolved from slot environment.
 * Tests skip gracefully when required values are missing.
 */

// TODO: declare env-var-backed constants for this module
// export const SOME_ID = process.env.<%= apiName.toUpperCase() %>_SOME_ID ?? '';
